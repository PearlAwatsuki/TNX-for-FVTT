<section class="scenario-sheet two-column-layout">
    <nav class="sheet-tabs vertical-tabs scenario-sidebar" data-group="primary">
        <a class="item" data-action="tab" data-tab="scenario-info" data-group="primary">
            <i class="fas fa-scroll"></i>
            <span>シナリオ情報</span>
        </a>
        <a class="item" data-action="tab" data-tab="scenes" data-group="primary">
            <i class="fas fa-film"></i>
            <span>シーン</span>
        </a>
        <a class="item" data-action="tab" data-tab="info" data-group="primary">
            <i class="fas fa-info-circle"></i>
            <span>情報項目</span>
        </a>
        <a class="item" data-action="tab" data-tab="requests" data-group="primary">
            <i class="fas fa-clipboard-list"></i>
            <span>RL プリセット</span>
        </a>
    </nav>

    <section class="sheet-body">
        <div class="tab scenario-info" data-group="primary" data-tab="scenario-info">
            <div class="scenario-info-container">
                <div class="act-name-section">
                    <label>アクト名</label>
                    <input type="text" name="documentName" value="{{documentName}}" placeholder="アクトの名前"/>
                </div>
                <div class="trailer-section tnx-editor-section">
                    <div class="tnx-editor-section__header">
                        <label>トレーラー</label>
                    </div>
                    <div class="tnx-editor-section__content">
                        <prose-mirror name="trailer" value="{{trailer}}" toggled>{{{trailer}}}</prose-mirror>
                    </div>
                </div>
                <div class="handouts-section">
                    <div class="tnx-section-header">
                        <h3>ハンドアウト</h3>
                        <button type="button" class="tnx-ring-btn" data-action="addHandout" title="ハンドアウトを追加">
                            <i class="fas fa-plus"></i>
                        </button>
                    </div>
                    <div class="handout-list">
                        {{#each handouts as |handout|}}
                        <div class="handout-item" data-id="{{handout.id}}">
                            {{!-- 1行目=ハンドアウト名(2026-08-09 裁定): 番号前置＋スタイルセレクト＋自動表示
                                  「①<スタイル名>用ハンドアウト」。共通=「共通ハンドアウト」(番号なし)・
                                  自由記述=記入欄を自動表示エリアまで伸長 --}}
                            <div class="handout-row handout-row--header">
                                {{#if handout.numberLabel}}<span class="handout-number">{{handout.numberLabel}}</span>{{/if}}
                                <select name="recommendedStyle" class="handout-style">
                                    <option value="">{{#if handout.legacyStyle}}旧: {{handout.legacyStyle}}{{else}}-{{/if}}</option>
                                    <option value="@common" {{#if handout.isCommon}}selected{{/if}}>共通</option>
                                    <option value="@free" {{#if handout.isFreeTitle}}selected{{/if}}>自由記述</option>
                                    <optgroup label="スタイル">
                                        {{#each handout.styleOptions as |opt|}}
                                            <option value="{{opt.value}}" {{#if opt.selected}}selected{{/if}}>{{opt.label}}</option>
                                        {{/each}}
                                    </optgroup>
                                </select>
                                {{#if handout.isFreeTitle}}
                                    <input type="text" name="title" class="handout-title" value="{{handout.title}}" placeholder="ハンドアウト名">
                                {{else}}
                                    <span class="handout-title-auto">{{handout.titleSuffix}}</span>
                                {{/if}}
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="deleteHandout" title="削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            {{!-- 各欄はラベルをコントロールに直付けした横並びの行(2026-08-09 指摘=
                                  ラベル列グリッドは短いラベルに無用の隙間を作るため不使用)。
                                  コネは必ず一つ=単一セレクト・推奨スートはコネに付随(同じ行)・
                                  対象ユーザーは内容でないため一番下 --}}
                            <div class="handout-row handout-row--meta">
                                <label>コネ</label>
                                <select name="actConnection" class="handout-conn-select">
                                    <option value="">{{#if handout.connections}}旧: {{handout.connections}}{{else}}-{{/if}}</option>
                                    {{#each handout.connOptions as |opt|}}
                                        <option value="{{opt.value}}" {{#if opt.selected}}selected{{/if}}>{{opt.label}}</option>
                                    {{/each}}
                                    {{#if handout.connBroken}}<option value="{{handout.actConnection}}" selected>（参照切れ）</option>{{/if}}
                                </select>
                                <label class="handout-suit-label">推奨スート</label>
                                <select name="recommendedSuit" class="handout-suit-select">
                                    <option value="">{{#if handout.legacySuit}}旧: {{handout.legacySuit}}{{else}}-{{/if}}</option>
                                    {{#each handout.suits as |opt|}}
                                        <option value="{{opt.value}}" {{#if opt.selected}}selected{{/if}}>{{opt.label}}</option>
                                    {{/each}}
                                </select>
                            </div>
                            <div class="tnx-editor-section">
                                <div class="tnx-editor-section__header">
                                    <label>ハンドアウト内容</label>
                                </div>
                                <div class="tnx-editor-section__content">
                                    <prose-mirror name="content" value="{{handout.content}}" toggled>{{{handout.content}}}</prose-mirror>
                                </div>
                            </div>
                            <div class="handout-row handout-row--ps">
                                <label>PS</label>
                                <input type="text" name="ps" value="{{handout.ps}}" placeholder="このハンドアウトの目的などを記載...">
                            </div>
                            <div class="handout-row handout-row--user">
                                <label>対象ユーザー</label>
                                <select name="userId">
                                    <option value="">{{#if handout.legacyCastName}}旧: {{handout.legacyCastName}}{{else}}-{{/if}}</option>
                                    {{#each handout.userOptions as |user|}}
                                        <option value="{{user.id}}" {{#if user.selected}}selected{{/if}}>{{user.name}}</option>
                                    {{/each}}
                                </select>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>

        <div class="tab scenes" data-group="primary" data-tab="scenes">
            <div class="scenes-container">
                {{#each scenes as |phaseScenes phaseKey|}}
                <div class="phase-section">
                    <div class="tnx-section-header">
                        <h3>{{lookup ../phaseLabels phaseKey}}</h3>
                    </div>
                    <div class="scene-table">
                        <div class="scene-table-header">
                            <div class="header-cell scene-number">No.</div>
                            <div class="header-cell scene-kind">種別</div>
                            <div class="header-cell scene-name">シーン名</div>
                            <div class="header-cell scene-player">シーンプレイヤー</div>
                            <div class="header-cell scene-stage">使用シーン</div>
                            <div class="header-cell scene-controls">
                                <button type="button" class="tnx-ring-btn" data-action="addScene" data-phase="{{phaseKey}}" title="このフェイズにシーンを追加">
                                    <i class="fas fa-plus"></i>
                                </button>
                            </div>
                        </div>
                        <div class="scene-table-body">
                            {{#each phaseScenes as |scene index|}}
                                <div class="scene-item" data-scene-id="{{scene.id}}" data-phase="{{phaseKey}}">
                                    <div class="scene-main-row">
                                        {{!-- No. は台本順の自動採番(14-8)。巡回シーンを含むと
                                              上演中のシーン数が変動するため手入力はしない
                                              （上演中の「SCENE n」は実行時のカウンタが出す） --}}
                                        <div class="table-cell scene-number">
                                            <span class="scene-number-auto">{{scene.seqNumber}}</span>
                                        </div>
                                        {{!-- 種別(14-8): 通常/巡回/イベント。台本を読んだだけで
                                              進行の形が分かるよう一覧の行に出す --}}
                                        <div class="table-cell scene-kind">
                                            <select name="kind">
                                                {{#each @root.sceneKindOptions as |opt|}}
                                                    <option value="{{opt.value}}" {{#if (eq opt.value scene.kind)}}selected{{/if}}>{{opt.label}}</option>
                                                {{/each}}
                                            </select>
                                        </div>
                                        <div class="table-cell scene-name">
                                            <input type="text" name="name" value="{{scene.name}}"/>
                                        </div>
                                        <div class="table-cell scene-player">
                                            {{#if (eq scene.kind "rotation")}}
                                                {{!-- 巡回シーンのシーンプレイヤーは入場のたびに
                                                      ハンドアウト順の未消化の先頭へ回る＝台本では決めない --}}
                                                <select data-no-save disabled><option>巡回</option></select>
                                            {{else}}
                                                {{!-- ルーラーシーンは独立した選択肢(2026-08-10)。
                                                      GM ユーザーの選択は「その RL のキャストが
                                                      主役」を意味し、ルーラーシーンではない --}}
                                                <select name="playerUserId">
                                                    <option value="">{{#if scene.player}}旧: {{scene.player}}{{else}}- 未選択 -{{/if}}</option>
                                                    <option value="{{@root.scenePlayerRuler}}" {{#if (eq @root.scenePlayerRuler scene.playerUserId)}}selected{{/if}}>ルーラーシーン</option>
                                                    {{#each ../../scenePlayerUsers as |user|}}
                                                        <option value="{{user.id}}" {{#if (eq user.id scene.playerUserId)}}selected{{/if}}>
                                                            {{user.name}}
                                                        </option>
                                                    {{/each}}
                                                </select>
                                            {{/if}}
                                        </div>
                                        <div class="table-cell scene-stage">
                                            <select name="stage">
                                                <option value="">- 使用シーンなし -</option>
                                                {{#if ../../stageSubSceneOptions.length}}
                                                    <optgroup label="サブシーン">
                                                        {{#each ../../stageSubSceneOptions as |opt|}}
                                                            <option value="{{opt.value}}" {{#if (eq opt.value scene.stage)}}selected{{/if}}>{{opt.label}}</option>
                                                        {{/each}}
                                                    </optgroup>
                                                {{/if}}
                                                {{#if ../../stageSceneOptions.length}}
                                                    <optgroup label="通常シーン">
                                                        {{#each ../../stageSceneOptions as |opt|}}
                                                            <option value="{{opt.value}}" {{#if (eq opt.value scene.stage)}}selected{{/if}}>{{opt.label}}</option>
                                                        {{/each}}
                                                    </optgroup>
                                                {{/if}}
                                            </select>
                                        </div>
                                        <div class="table-cell scene-controls">
                                            <a class="tnx-icon-ctrl" data-action="deleteScene" title="このシーンを削除"><i class="fas fa-trash"></i></a>
                                        </div>
                                    </div>
                                    <details class="scene-details-row">
                                        <summary>詳細設定</summary>
                                        <div class="scene-details-body">
                                            {{!-- 「詳細設定」の折りたたみ自体がグループ=内側に枠は作らない。
                                                  登場設定は横並びの行に置く(2026-08-09 指摘=縦積みのみでは右が空く)。
                                                  ラベルは各コントロールに直付け --}}
                                            {{!-- イベントシーンの起動条件(14-8): RL が読んで判断する
                                                  ための覚え書き。条件の自動判定は行わない --}}
                                            {{#if (eq scene.kind "event")}}
                                                <div class="scene-detail-row">
                                                    <label>起動条件</label>
                                                    <input type="text" name="eventCondition" value="{{scene.eventCondition}}"/>
                                                </div>
                                            {{/if}}
                                            {{!-- 巡回シーンはエリア・登場判定を台本で持たない
                                                  (常に「未設定」＝シーン開始ダイアログで決める) --}}
                                            {{#unless (eq scene.kind "rotation")}}
                                                <div class="scene-detail-row">
                                                    <label>エリア</label>
                                                    <select name="area">
                                                        {{#each @root.sceneAreaOptions as |opt|}}
                                                            <option value="{{opt.value}}" {{#if (eq opt.value scene.area)}}selected{{/if}}>{{opt.label}}</option>
                                                        {{/each}}
                                                    </select>
                                                    <label class="scene-detail-label--next">登場判定</label>
                                                    <select name="appearanceMode">
                                                        <option value="area" {{#if (eq scene.appearanceMode "area")}}selected{{/if}}>エリア準拠</option>
                                                        <option value="fixed" {{#if (eq scene.appearanceMode "fixed")}}selected{{/if}}>数値指定</option>
                                                        <option value="unset" {{#if (eq scene.appearanceMode "unset")}}selected{{/if}}>未設定</option>
                                                        <option value="none" {{#if (eq scene.appearanceMode "none")}}selected{{/if}}>不可</option>
                                                    </select>
                                                    {{#if (eq scene.appearanceMode "fixed")}}
                                                        <div class="number-input-spinner">
                                                            <button type="button" class="tnx-btn" data-action="spinDown" aria-label="減少">−</button>
                                                            <input type="number" name="appearanceValue" value="{{scene.appearanceValue}}" min="0"/>
                                                            <button type="button" class="tnx-btn" data-action="spinUp" aria-label="増加">＋</button>
                                                        </div>
                                                    {{/if}}
                                                </div>
                                            {{/unless}}
                                            {{!-- 登場不可のシーンは登場判定そのものが起きない=指定技能は出さない
                                                  (2026-08-09 指摘。既存の指定はデータとして保持し、モードを戻せば再表示) --}}
                                            {{#unless (eq scene.appearanceMode "none")}}
                                                {{!-- 複数選択のタグ入力。ラベルは**プルダウン側**に付け(チップは 0 個に
                                                      なりうるのでラベルの相手にならない)、プルダウンは全幅・チップは
                                                      その下(2026-08-09 ユーザー指示)。2行の内容列は縦に揃える --}}
                                                <div class="scene-detail-row scene-tag-row">
                                                    <label>指定技能</label>
                                                    <div class="scene-tag-field">
                                                        {{#if scene.appearanceSkillChips.length}}
                                                            <div class="tnx-tag-list">
                                                                {{#each scene.appearanceSkillChips as |chip|}}
                                                                    <span class="tnx-tag" data-key="{{chip.key}}">{{chip.name}}<a class="tnx-tag-remove" data-action="removeAppearanceSkill" data-key="{{chip.key}}" title="指定を外す"><i class="fas fa-times"></i></a></span>
                                                                {{/each}}
                                                            </div>
                                                        {{/if}}
                                                        <select data-no-save class="appearance-skill-select">
                                                            <option value="">＋ 技能を追加</option>
                                                            {{#each @root.skillGroupsPlain as |group|}}
                                                                <optgroup label="{{group.label}}">
                                                                    {{#each group.skills as |skill|}}
                                                                        <option value="{{skill.identificationKey}}">{{skill.name}}</option>
                                                                    {{/each}}
                                                                </optgroup>
                                                            {{/each}}
                                                        </select>
                                                    </div>
                                                </div>
                                            {{/unless}}
                                            {{!-- 登場キャラクターの事前設定(14-8): シーン入場時に
                                                  登場判定なしで登場する面々。目のトグル＝名前を伏せて
                                                  登場(卓には「？？？」)。RL の指定なので「登場：不可」
                                                  のシーンでも有効＝登場判定の設定の外に置く --}}
                                            <div class="scene-detail-row scene-tag-row">
                                                <label>登場</label>
                                                <div class="scene-tag-field">
                                                    {{#if scene.appearanceActorChips.length}}
                                                        <div class="tnx-tag-list">
                                                            {{#each scene.appearanceActorChips as |chip|}}
                                                                <span class="tnx-tag" data-actor-id="{{chip.actorId}}">{{chip.name}}<a class="tnx-tag-toggle {{#if chip.hideName}}is-on{{/if}}" data-action="toggleAppearanceActorHidden" data-actor-id="{{chip.actorId}}" title="{{#if chip.hideName}}名前を伏せて登場{{else}}名前を出して登場{{/if}}"><i class="fas {{#if chip.hideName}}fa-eye-slash{{else}}fa-eye{{/if}}"></i></a><a class="tnx-tag-remove" data-action="removeAppearanceActor" data-actor-id="{{chip.actorId}}" title="指定を外す"><i class="fas fa-times"></i></a></span>
                                                            {{/each}}
                                                        </div>
                                                    {{/if}}
                                                    <select data-no-save class="appearance-actor-select">
                                                        <option value="">＋ キャラクターを追加</option>
                                                        {{#each @root.appearanceActorGroups as |group|}}
                                                            <optgroup label="{{group.label}}">
                                                                {{#each group.actors as |actor|}}
                                                                    <option value="{{actor.id}}">{{actor.name}}</option>
                                                                {{/each}}
                                                            </optgroup>
                                                        {{/each}}
                                                    </select>
                                                </div>
                                            </div>
                                            <div class="tnx-editor-section">
                                                <div class="tnx-editor-section__header">
                                                    <label>切り替えメッセージ</label>
                                                </div>
                                                <div class="tnx-editor-section__content">
                                                    <prose-mirror name="switchMessage" value="{{scene.switchMessage}}" toggled>{{{scene.switchMessage}}}</prose-mirror>
                                                </div>
                                            </div>
                                        </div>
                                    </details>
                                </div>
                            {{/each}}
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>

        <div class="tab info" data-group="primary" data-tab="info">
            <div class="info-container">
                <div class="tnx-section-header">
                    <h3>情報項目</h3>
                    <button type="button" class="tnx-btn tnx-btn--sm" data-action="addInfoItem" title="情報項目を追加">
                        <i class="fas fa-plus"></i> 情報を追加
                    </button>
                </div>
                <div class="info-item-list">
                    {{#each infoItems as |infoItem|}}
                        <div class="info-item" data-id="{{infoItem.id}}">
                            {{!-- 公開/非公開・調査済みの操作はシナリオコントロールパネルの領分(シートは台本編集専用) --}}
                            <div class="tnx-item-header tnx-item-header--card">
                                <input type="text" name="title" value="{{infoItem.title}}" placeholder="情報名" data-info-id="{{infoItem.id}}"/>
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="deleteInfoItem" data-info-id="{{infoItem.id}}" title="この項目を削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            <div class="info-item-body">
                                <div class="info-contents-list">
                                    {{#each infoItem.contents as |contentItem|}}
                                        <div class="info-content-block" data-content-id="{{contentItem.id}}">
                                            <a class="tnx-icon-ctrl info-content-delete" data-action="deleteInfoContent" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" title="この情報内容を削除"><i class="fas fa-trash"></i></a>
                                            <div class="skill-checks-area">
                                                <div class="tnx-item-header">
                                                    <h5>使用技能</h5>
                                                    {{!-- 行の追加＝目標値の異なる技能の指定(行内の「＋ 技能を追加」と役割が違う) --}}
                                                    <a class="tnx-ring-btn" data-action="addSkillCheck" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" title="目標値の異なる技能を追加"><i class="fas fa-plus"></i></a>
                                                </div>
                                                <div class="skill-check-list">
                                                    {{#each contentItem.skills as |skillItem|}}
                                                    <div class="skill-check-row" data-skill-id="{{skillItem.id}}">
                                                        {{!-- 使用技能は辞典のタグ入力(2026-08-09 裁定=自由記述は廃止)。
                                                              1行＝技能の集合＋共通の目標値で、選ぶと行の中に積み上がる。
                                                              目標値の異なる技能はこの行そのものを足して指定する --}}
                                                        <div class="tnx-tag-field">
                                                            <div class="tnx-tag-list">
                                                                {{#each skillItem.skillTags as |tag|}}
                                                                    <span class="tnx-tag" data-key="{{tag.key}}">{{tag.name}}<a class="tnx-tag-remove" data-action="removeInfoSkill" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" data-skill-id="{{skillItem.id}}" data-key="{{tag.key}}" title="外す"><i class="fas fa-times"></i></a></span>
                                                                {{/each}}
                                                                {{#if skillItem.legacyName}}
                                                                    <span class="tnx-tag">旧: {{skillItem.legacyName}}<a class="tnx-tag-remove" data-action="removeInfoSkill" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" data-skill-id="{{skillItem.id}}" data-key="" title="外す"><i class="fas fa-times"></i></a></span>
                                                                {{/if}}
                                                                {{#unless skillItem.skillTags.length}}{{#unless skillItem.legacyName}}<span class="tnx-tag-empty">（未設定）</span>{{/unless}}{{/unless}}
                                                            </div>
                                                            <select data-no-save class="info-skill-add tnx-tag-add" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" data-skill-id="{{skillItem.id}}">
                                                                <option value="">＋ 技能を追加</option>
                                                                {{#each @root.skillGroupsPlain as |group|}}
                                                                    <optgroup label="{{group.label}}">
                                                                        {{#each group.skills as |skill|}}
                                                                            <option value="{{skill.identificationKey}}">{{skill.name}}</option>
                                                                        {{/each}}
                                                                    </optgroup>
                                                                {{/each}}
                                                            </select>
                                                        </div>
                                                        {{!-- 目標値と削除は追加プルダウンと同じ行(カードの一番下)に置く --}}
                                                        <div class="skill-check-target">
                                                            <span class="tn-label">目標値:</span>
                                                            <div class="number-input-spinner">
                                                                <button type="button" class="tnx-btn" data-action="spinDown" aria-label="減少">−</button>
                                                                <input type="number" name="tn" class="skill-tn" placeholder="-" value="{{skillItem.tn}}" min="0" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" data-skill-id="{{skillItem.id}}">
                                                                <button type="button" class="tnx-btn" data-action="spinUp" aria-label="増加">＋</button>
                                                            </div>
                                                            <a class="tnx-ring-btn" data-action="deleteSkillCheck" data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}" data-skill-id="{{skillItem.id}}" title="この技能を削除"><i class="fas fa-times"></i></a>
                                                        </div>
                                                    </div>
                                                    {{/each}}
                                                </div>
                                            </div>
                                            <div class="tnx-editor-section">
                                                <div class="tnx-editor-section__header">
                                                    <label>本文</label>
                                                </div>
                                                <div class="tnx-editor-section__content">
                                                    <prose-mirror name="text" value="{{contentItem.text}}" toggled data-info-id="{{infoItem.id}}" data-content-id="{{contentItem.id}}">{{{contentItem.text}}}</prose-mirror>
                                                </div>
                                            </div>
                                        </div>
                                    {{/each}}
                                </div>
                            </div>
                            <div class="add-content-button-wrapper">
                                <button type="button" class="tnx-btn tnx-btn-dashed" data-action="addInfoContent" data-info-id="{{infoItem.id}}">
                                    <i class="fas fa-plus"></i> 情報の内容を追加
                                </button>
                            </div>
                        </div>
                    {{else}}
                        <div class="no-items-placeholder">
                            <p>情報項目がありません。「情報を追加」ボタンから作成してください。</p>
                        </div>
                    {{/each}}
                </div>
            </div>
        </div>

        <div class="tab requests" data-group="primary" data-tab="requests">
            <div class="request-preset-container">

                {{!-- シナリオテキストは RL が卓で送る素材＝他のプリセットと同じ扱い(2026-08-09 に
                      テキストタブから合流)。保存先フラグは scenarioTexts のまま --}}
                <div class="tnx-section-header">
                    <h3>シナリオテキスト</h3>
                    <button type="button" class="tnx-btn tnx-btn--sm" data-action="addTextPreset">
                        <i class="fas fa-plus"></i> テキストを追加
                    </button>
                </div>
                <div class="preset-list">
                    {{#each scenarioTexts}}
                        <div class="preset-item" data-preset-id="{{this.id}}">
                            <div class="tnx-item-header tnx-item-header--card">
                                {{!-- 名前欄は他のプリセットの label と同じ役割だが、既存データのキー名 title を保つ --}}
                                <input type="text" name="title" value="{{this.title}}" placeholder="{{this.placeholder}}"
                                       data-preset-kind="scenarioTexts" data-preset-id="{{this.id}}">
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="presetUp"   data-preset-kind="scenarioTexts" data-preset-id="{{this.id}}" title="上へ"><i class="fas fa-chevron-up"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="presetDown" data-preset-kind="scenarioTexts" data-preset-id="{{this.id}}" title="下へ"><i class="fas fa-chevron-down"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="deleteTextPreset" data-preset-id="{{this.id}}" title="削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            <div class="preset-body">
                                <div class="tnx-editor-section">
                                    <div class="tnx-editor-section__header">
                                        <label>内容</label>
                                    </div>
                                    <div class="tnx-editor-section__content">
                                        <prose-mirror name="content" value="{{this.content}}" toggled
                                                      data-preset-kind="scenarioTexts" data-preset-id="{{this.id}}">{{{this.content}}}</prose-mirror>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {{else}}
                        <div class="no-items-placeholder">
                            <p>シナリオテキストがありません。「テキストを追加」から作成してください。</p>
                        </div>
                    {{/each}}
                </div>

                <div class="tnx-section-header">
                    <h3>判定要求</h3>
                    <button type="button" class="tnx-btn tnx-btn--sm" data-action="addCheckRequestPreset">
                        <i class="fas fa-plus"></i> 判定要求を追加
                    </button>
                </div>
                <div class="preset-list">
                    {{#each checkRequestPresets}}
                        <div class="preset-item" data-preset-id="{{this.id}}">
                            <div class="tnx-item-header tnx-item-header--card">
                                <input type="text" name="label" value="{{this.label}}" placeholder="{{this.placeholder}}"
                                       data-preset-kind="checkRequests" data-preset-id="{{this.id}}">
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="presetUp"   data-preset-kind="checkRequests" data-preset-id="{{this.id}}" title="上へ"><i class="fas fa-chevron-up"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="presetDown" data-preset-kind="checkRequests" data-preset-id="{{this.id}}" title="下へ"><i class="fas fa-chevron-down"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="deletePreset" data-preset-kind="checkRequests" data-preset-id="{{this.id}}" title="削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            <div class="preset-body tnx-form-field">
                                <div class="form-group">
                                    <label>判定種別</label>
                                    <select name="checkType" data-preset-kind="checkRequests" data-preset-id="{{this.id}}">
                                        {{#each this.checkTypes}}
                                            <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>{{this.label}}</option>
                                        {{/each}}
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>技能</label>
                                    <select name="identificationKey" data-preset-kind="checkRequests" data-preset-id="{{this.id}}">
                                        <option value="">（その他・自由入力）</option>
                                        {{#each this.skillGroups}}
                                            <optgroup label="{{this.label}}">
                                                {{#each this.skills}}
                                                    <option value="{{this.identificationKey}}" {{#if this.selected}}selected{{/if}}>{{this.name}}</option>
                                                {{/each}}
                                            </optgroup>
                                        {{/each}}
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>目標値</label>
                                    <div class="number-input-spinner">
                                        <button type="button" class="tnx-btn" data-action="spinDown" aria-label="減少">−</button>
                                        <input type="number" name="targetValue" value="{{this.targetValue}}" min="0" max="99"
                                               data-preset-kind="checkRequests" data-preset-id="{{this.id}}">
                                        <button type="button" class="tnx-btn" data-action="spinUp" aria-label="増加">＋</button>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="rl-check-label">
                                        <input type="checkbox" name="targetValueHidden" {{checked this.targetValueHidden}}
                                               data-preset-kind="checkRequests" data-preset-id="{{this.id}}">
                                        <span>目標値をプレイヤーに非公開</span>
                                    </label>
                                </div>
                                <div class="form-group">
                                    <label>説明</label>
                                    <input type="text" name="description" value="{{this.description}}"
                                           data-preset-kind="checkRequests" data-preset-id="{{this.id}}">
                                </div>
                            </div>
                        </div>
                    {{else}}
                        <div class="no-items-placeholder">
                            <p>判定要求がありません。「判定要求を追加」から作成してください。</p>
                        </div>
                    {{/each}}
                </div>

                <div class="tnx-section-header">
                    <h3>報酬点</h3>
                    <button type="button" class="tnx-btn tnx-btn--sm" data-action="addBountyPreset">
                        <i class="fas fa-plus"></i> 報酬点を追加
                    </button>
                </div>
                <div class="preset-list">
                    {{#each bountyPresets}}
                        <div class="preset-item" data-preset-id="{{this.id}}">
                            <div class="tnx-item-header tnx-item-header--card">
                                <input type="text" name="label" value="{{this.label}}" placeholder="{{this.placeholder}}"
                                       data-preset-kind="bountyGrants" data-preset-id="{{this.id}}">
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="presetUp"   data-preset-kind="bountyGrants" data-preset-id="{{this.id}}" title="上へ"><i class="fas fa-chevron-up"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="presetDown" data-preset-kind="bountyGrants" data-preset-id="{{this.id}}" title="下へ"><i class="fas fa-chevron-down"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="deletePreset" data-preset-kind="bountyGrants" data-preset-id="{{this.id}}" title="削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            <div class="preset-body tnx-form-field">
                                <div class="form-group">
                                    <label>点数</label>
                                    <div class="number-input-spinner">
                                        <button type="button" class="tnx-btn" data-action="spinDown" aria-label="減少">−</button>
                                        <input type="number" name="amount" value="{{this.amount}}" min="-99" max="99"
                                               data-preset-kind="bountyGrants" data-preset-id="{{this.id}}">
                                        <button type="button" class="tnx-btn" data-action="spinUp" aria-label="増加">＋</button>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label>記述</label>
                                    <input type="text" name="note" value="{{this.note}}"
                                           data-preset-kind="bountyGrants" data-preset-id="{{this.id}}">
                                </div>
                            </div>
                        </div>
                    {{else}}
                        <div class="no-items-placeholder">
                            <p>報酬点がありません。「報酬点を追加」から作成してください。</p>
                        </div>
                    {{/each}}
                </div>

                <div class="tnx-section-header">
                    <h3>ダメージ</h3>
                    <button type="button" class="tnx-btn tnx-btn--sm" data-action="addDamageGrantPreset">
                        <i class="fas fa-plus"></i> ダメージを追加
                    </button>
                </div>
                <div class="preset-list">
                    {{#each damageGrantPresets}}
                        <div class="preset-item" data-preset-id="{{this.id}}">
                            <div class="tnx-item-header tnx-item-header--card">
                                <input type="text" name="label" value="{{this.label}}" placeholder="{{this.placeholder}}"
                                       data-preset-kind="damageGrants" data-preset-id="{{this.id}}">
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="presetUp"   data-preset-kind="damageGrants" data-preset-id="{{this.id}}" title="上へ"><i class="fas fa-chevron-up"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="presetDown" data-preset-kind="damageGrants" data-preset-id="{{this.id}}" title="下へ"><i class="fas fa-chevron-down"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="deletePreset" data-preset-kind="damageGrants" data-preset-id="{{this.id}}" title="削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            <div class="preset-body tnx-form-field">
                                <div class="form-group">
                                    <label>系統</label>
                                    <select name="category" data-preset-kind="damageGrants" data-preset-id="{{this.id}}">
                                        {{#each this.categories}}
                                            <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>{{this.label}}</option>
                                        {{/each}}
                                    </select>
                                </div>
                                {{!-- 決め方（固定／カードを出す通常算出・2026-07-24） --}}
                                <div class="form-group">
                                    <label>決め方</label>
                                    <select name="mode" data-preset-kind="damageGrants" data-preset-id="{{this.id}}">
                                        {{#each this.modes}}
                                            <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>{{this.label}}</option>
                                        {{/each}}
                                    </select>
                                </div>
                                {{!-- ダメージ＝種別＋値の1項目（攻撃力の設定と同じ split-fields）。
                                      値ラベルはモードに追随（固定＝ダメージ／カード＝基準値） --}}
                                <div class="form-group damage-fields-group">
                                    <label class="damage-value-label">{{this.valueLabel}}</label>
                                    <div class="form-fields split-fields">
                                        <select name="damageType" class="damage-type-field"
                                                data-preset-kind="damageGrants" data-preset-id="{{this.id}}">
                                            {{#each this.damageTypes}}
                                                <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>{{this.label}}</option>
                                            {{/each}}
                                        </select>
                                        <div class="number-input-spinner">
                                            <button type="button" class="tnx-btn" data-action="spinDown" aria-label="減少">−</button>
                                            <input type="number" name="value" value="{{this.value}}" min="0" max="99"
                                                   data-preset-kind="damageGrants" data-preset-id="{{this.id}}">
                                            <button type="button" class="tnx-btn" data-action="spinUp" aria-label="増加">＋</button>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label>記述</label>
                                    <input type="text" name="note" value="{{this.note}}"
                                           data-preset-kind="damageGrants" data-preset-id="{{this.id}}">
                                </div>
                            </div>
                        </div>
                    {{else}}
                        <div class="no-items-placeholder">
                            <p>ダメージがありません。「ダメージを追加」から作成してください。</p>
                        </div>
                    {{/each}}
                </div>

                <div class="tnx-section-header">
                    <h3>効果</h3>
                    <button type="button" class="tnx-btn tnx-btn--sm" data-action="addEffectGrantPreset">
                        <i class="fas fa-plus"></i> 効果を追加
                    </button>
                </div>
                <div class="preset-list">
                    {{#each effectGrantPresets}}
                        <div class="preset-item" data-preset-id="{{this.id}}">
                            <div class="tnx-item-header tnx-item-header--card">
                                <input type="text" name="label" value="{{this.label}}" placeholder="{{this.placeholder}}"
                                       data-preset-kind="effectGrants" data-preset-id="{{this.id}}">
                                <div class="tnx-ctrl-strip">
                                    <a class="tnx-icon-ctrl" data-action="presetUp"   data-preset-kind="effectGrants" data-preset-id="{{this.id}}" title="上へ"><i class="fas fa-chevron-up"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="presetDown" data-preset-kind="effectGrants" data-preset-id="{{this.id}}" title="下へ"><i class="fas fa-chevron-down"></i></a>
                                    <a class="tnx-icon-ctrl" data-action="deletePreset" data-preset-kind="effectGrants" data-preset-id="{{this.id}}" title="削除"><i class="fas fa-trash"></i></a>
                                </div>
                            </div>
                            <div class="preset-body tnx-form-field">
                                {{!-- 効果の見せ方はシステム標準の効果リストに揃える --}}
                                <div class="tnx-item-list tnx-item-list--effects">
                                    <div class="tnx-item-list__row">
                                        <div class="tnx-item-list__column--main">
                                            <img class="effect-image" src="{{this.effect.img}}" alt="{{this.effect.name}}"/>
                                            <h4 class="{{#unless this.effect.set}}disabled{{/unless}}">{{this.effect.name}}</h4>
                                        </div>
                                        <div class="tnx-item-list__column--summary">{{this.effect.summary}}</div>
                                        <div class="tnx-item-list__column--controls">
                                            {{#if this.effect.set}}
                                                <a class="tnx-icon-ctrl" data-action="editEffectPreset"
                                                   data-preset-id="{{this.id}}" title="編集"><i class="fas fa-edit"></i></a>
                                            {{else}}
                                                <a class="tnx-icon-ctrl" data-action="editEffectPreset"
                                                   data-preset-id="{{this.id}}" title="作成"><i class="fas fa-plus"></i></a>
                                            {{/if}}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {{else}}
                        <div class="no-items-placeholder">
                            <p>効果がありません。「効果を追加」から作成してください。</p>
                        </div>
                    {{/each}}
                </div>

            </div>
        </div>
    </section>
</section>
