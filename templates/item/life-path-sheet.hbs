<section class="{{cssClass}}">
    <header class="sheet-header">
        <img class="profile-img" src="{{item.img}}" data-edit="img" data-action="editImage" title="{{item.name}}"/>

        <div class="header-fields">
            <h1 class="charname">
                <input name="name" type="text" value="{{item.name}}" placeholder="ライフパス名" />
            </h1>
        </div>
    </header>

    <nav class="sheet-tabs tabs" data-group="primary">
        <a class="item" data-action="tab" data-group="primary" data-tab="description">説明</a>
        <a class="item" data-action="tab" data-group="primary" data-tab="setting">設定</a>
    </nav>

    <section class="sheet-body">
        <div class="tab description" data-group="primary" data-tab="description">
            <div class="tnx-editor-section">
                <div class="tnx-editor-section__header">
                    <label>説明</label>
                </div>
                <div class="tnx-editor-section__content">
                    {{editor enrichedDescription target="system.description" engine="prosemirror" button=true owner=owner editable=editable}}
                </div>
            </div>
        </div>

        <div class="tab setting" data-group="primary" data-tab="setting">
            <fieldset class="tnx-fieldset">
                <legend>ライフパス設定</legend>
                <div class="tnx-form-field">
                    <div class="form-group">
                        <label>種別</label>
                        <select name="system.lifePathType">
                            <option value="" {{#unless system.lifePathType}}selected{{/unless}}>—</option>
                            <option value="origin"     {{#if (eq system.lifePathType "origin")}}selected{{/if}}>出自</option>
                            <option value="experience" {{#if (eq system.lifePathType "experience")}}selected{{/if}}>経験</option>
                            <option value="encounter"  {{#if (eq system.lifePathType "encounter")}}selected{{/if}}>邂逅</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>習得技能</label>
                        <input type="text" name="system.skillName" value="{{system.skillName}}" placeholder="技能名を入力"/>
                    </div>
                </div>
            </fieldset>
            <fieldset class="tnx-fieldset">
                <legend>識別キー</legend>
                <div class="tnx-form-field">
                    <div class="form-group">
                        <label>識別キー</label>
                        <input type="text" name="system.identificationKey" value="{{system.identificationKey}}" placeholder="識別キーを入力"/>
                    </div>
                </div>
            </fieldset>
        </div>
    </section>
</section>
