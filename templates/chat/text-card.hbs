{{!-- 長文送信カード(2026-08-15)。トレーラーとシナリオテキストで共用する——どちらも
      「種別タグ＋名前＋入力された長文」で構造が同じもの。
      骨格・段は統一規格の部品(tnxCard/tnxCardText)で組む(2026-09-05)。
      名前は付いているときだけ出す(無名のテキストはタグだけで足りる) --}}
{{#> tnxCard cardClass="tnx-text-card" typeLabel=typeLabel title=title}}
    {{> tnxCardText content=content}}
{{/tnxCard}}
