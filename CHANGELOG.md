# Changelog

このプロジェクトの全ての注目すべき変更はこのファイルに記録される。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [Unreleased]

### Added
- チャットカードの種別タグを**固定幅**にした（2026-09-06 ユーザー指示）。幅の基準は一番長い種別名
  「アーティフィシャルボディ」（12文字）が潰れずに収まる長さ。それより長い種別名が来たときだけ**横に縮めて**
  収める（`fitCardTags`）。長い種別名で本体である名前を押し出さないための処置。
- **チャットカードの形式を統一規格にした**（2026-09-05 ユーザー指示「そもそもチャットカードの形式を完全に統一規格に
  してください。別々の場所で別々に作っているからこういうことになるのではないですか？」）。カードは19枚あり、骨格が
  2系統（送信系・結果系）に分かれ、同じ役割の段に2〜3つの名前があり、長文を畳めるかどうかがカードごとの判断だった。
  - 骨格と段を共通部品にした（`templates/chat/parts/card*.hbs`＝`tnxCard` と段5種）。全19枚をこの部品で組み直し、
    器・見出し・段のマークアップを各所で書く箇所を無くした。JS からライブ描画する段も `scripts/module/chat-card.mjs`
    の共通部品を通す
  - 見出しは全カードで「名前＋種別タグ（右寄せ）」。効果文などの長文を持つカードは**見出しそのものが開閉の起点**（解説カードが
    以前から採っていた作法。畳むための見出しを別に足さない）
  - 同じ役割に複数あった名前を1つにした（ラベル＝値の行が2つ、帰結の行が3つ、対象一覧が req 名のまま等）。
    クラス名は `tnx-card__*` に統一
  - JS で素の HTML を組んでいた5経路（アイテム解説・スタイル解説・NPC 取得・派生ダメージほか）をテンプレートへ移した
  - **過去ログの互換**: チャットカードは投稿時の HTML が保存されるため、旧クラス名のカードが卓のログに残る。
    統一規格の規則に旧名を別名として並べ（規則は1つのまま）、骨格ごと変わったカードは当時の意匠を互換の節に残した
  - 長文の畳みは**カードの見出しそのものが開閉の起点**（解説カードが以前から採っていた作法）。畳むための見出しを
    足さない（2026-09-05 ユーザー指摘「『効果文』とかいう謎見出し辞めれます？」「カードのヘッダーそのものが
    見出しで、折り畳み、展開もそこが起点だったはずだ」）
  - 種別タグは**卓に意味のある種別があるときだけ**出す。FVTT の文書型（「一般アウトフィット」等）は出さない
    （2026-09-05 ユーザー指摘）。アイテムの解説カードは名前だけ、スタイルの解説は在ゲームの区分として「スタイル」
- 神業のチャットカードの効果文（＋条件）を**畳めるようにした**（他のチャットカードと同じ details/summary。2026-09-05 ユーザー指摘
  「なぜ神業のチャットカードの効果文だけ折りたためないのですか？」）。**常にカード最上部に畳んだ状態で置く**——神業は
  判定と違って組み合わせが無く、効果文は読みたいときに開けば足りるため、使用の種類で開閉を変えない
  （2026-09-05 ユーザー指示。当初は宣言のときだけ開く実装だったものを統一）。

### Changed
- 神業の使用カードは素の HTML 組み立てから専用テンプレートへ移行（万能神業の「他の神業として使う」旧ダイアログは撤去。派生神業の選択は後続で作り直す）

### Removed
- 神業アイテムの区分フラグ `isKill` / `isDefence` / `isAll`（読み手も設定欄も無い形骸化フィールド。挙動の区分は用途側で表す。既存データの当該キーは次回保存時に捨てられる）

### Added
- **対象条件によるダメージ・効果の無効化**: 「ウェットの対象には効果がない」等、対象のありようで効果が働かない記述を扱えるようにした。粒度は4つ——①ダメージ修正の**行ごと**の対象条件（なし／ウェット／スタイル／ワークス × 「には無効」／「のみ有効」）②用途自身の修正値の対象条件 ③式の `@target.isWet`（対象がウェットなら1）④用途トグル「ウェットの対象には効果がない」（その用途のダメージ全体を0にし、適用効果の付与も行わない）。無効化された行は内訳から消さず「値0＋理由」で残す
- **AE のダメージ対象バフにウェット系を追加**: `damage.vsWet[.系統]` / `damage.vsNotWet[.系統]`（既存 `damage.vsStyle`/`vsWorks` の対称）。キー候補にも載る
- ウェット判定は電子妨害のウェット分岐と同一定義（準備中のアウトフィットに識別キー `background_wet`）

### Changed
- **技能・神業を狙う効果はキャラクターに付与するようにした**。これまでは「〈射撃〉のレベル+1」のような効果も武器のエンチャントと同じく技能アイテムへ転送していたが、技能のレベルはキャラクターが持つ技能のレベルであってモノの性能ではない。効果はキャラクター側に置いたまま技能の実効値へ届く。用途の「適用される効果」で技能・神業を狙う場合も付与先を選ばずキャラクターへ付与され、持続時間どおりに失効する。アウトフィット（武器・防具等）への効果は従来どおり対象アイテムへ転送される。**旧方式で技能アイテムの上に残っていた転送コピーは、RL の初回ロード時に一回だけ自動で除去される**（残すと効果が二重に乗るため）
- **複数対象への攻撃を対象ごとに評価するようにした**: 一括なのは判定（カードプレイ・達成値）とダメージカードの値・攻撃力・手動修正までで、**ダメージ修正は対象ごとに評価**される（対象条件・`@target.*` の式・`damage.vs*`）。ダメージカードの台帳は全対象で同じ行だけを共有表示し、対象で異なる行は各対象の内訳に出る。適用ダイアログ・適用済み表示も対象ごとの内訳を持つ。単体対象・対象非依存の式では従来と同じ見た目

### Fixed
- **神業の「母数-1」が効かず、多重取得の神業がアイテムごと消えていた**のを修正（2026-09-05）。`preDeleteItem`
  フックが async だったため `return false` が Promise になり削除を止められず、止めたつもりの更新が「もう無い文書」に
  当たってコンソールへ `Cannot read properties of undefined (reading 'id')` が出ていた（KI-051）。pre 系フックを
  同期にし、母数の判定は純関数 `miracleRemovalUpdate` に切り出した
- **他のアイテムを強化する効果を持つアイテムを追加しても、その場では効果が乗らなかった**のを修正（辞典から「特定の武器を強化するドラッグ」等をドラッグしても、別のアイテムを足すか効果を開いて保存し直すまで無言で効かなかった）。追加されたアイテムは狙われる側にも供給元側にもなりうるものとして扱う
- **複数のアイテムをまとめて追加すると、アイテムを狙う効果の転送が個数分だけ多重に乗っていた**のを修正（辞典から4つまとめてドロップすると、対象の武器に同じ強化が4回乗って攻撃力 5→13 になっていた）。転送の実体化をアクター単位で直列化し、「供給元×アイテムごとに転送は1つ」を不変条件にした。既に多重になっているデータは、その供給元の効果を次に変更したときに1つへ畳まれる
- **アイテムに付与された効果が持続時間を無視して残り続けていた**のを修正。用途の「適用される効果」がアイテムへ着地したとき（武器の攻撃力を上げる等）、その付与だけが境界（メインプロセス／カット／シーン／アクトの終わり）で失効せず恒久化していた。効果一覧には「シーン中」と表示されるのに消えない状態だった。アイテムに設定した効果そのものと、他アイテムから転送された効果は従来どおり残る
- ダメージ台帳の内訳で、供給元名が長い行の値が見切れることがあったのを折り返し表示に修正
- 複数対象を一括で攻撃したとき、対象依存のダメージ修正が先頭の命中対象の条件で全対象に適用されていたのを修正（上記の対象ごと評価による）

## [0.16.0] - 2026-09-01

フェーズ16（辞典ブラウザ＋購入・改造判定UI）完了。ゲームデータを引く・買う・改造するための基盤——ルールブックのデータページを踏襲した辞典ブラウザと、そこから地続きの購入判定・改造判定を作り込んだ。

### Added
- **辞典ブラウザ**: 辞典タブ上部のボタンから開く汎用データブラウザ。スタイル技能／アウトフィット／一般技能／スタイル／神業／オーガニゼーション／ライフパス／NPC の8タブ・名前検索・条件絞り込み（分類・ワークス・所属組織・購入値レンジ）。スタイル技能とアウトフィットはルールブックのデータカード形式（略号グリッド＝実紙面の型別セル幅配置・入りきらない値は文字幅圧縮）を踏襲した固定サイズカード、他タブも種別に合わせたカード・表形式。カードはワールドやシートへドラッグでインポートできる
- **アイテムのカード型ツールチップ**: シート上の技能・アウトフィット・神業・スタイル行にカーソルオンで辞典ブラウザと同形式のカードを表示
- **複数分類**: アウトフィットに副分類を追加し「複数の分類を持つアウトフィット」を正確に表現（チェックボックス展開UI・全読者が分類集合で判定・旧 isCyber フラグは副分類サイバーウェアへ完全統合）。製作技能に「対応分類」欄を追加
- **購入判定**: 用途タイプ「購入」を新設（〈信用〉も購入効果を持つスタイル技能も同型）。辞典ブラウザの購入ボタン、または技能から起動（アウトフィットのみの選択ブラウザ→判定へ合流）。カード判定（目標値＝購入値）／カードを出さない購入（外界＋消費報酬点・報酬点は実減算）／購入値が外界以下の常時入手の3経路。成功で辞典から複製を付与し、判定入手は常備化経験点を計上しない
- **プレアクト購入**: アクト未開始時のブラウザ購入は「購入値≤外界・常備化経験点あり」を条件とするプレアクト購入（経験点を支払わない所持）になり、条件外はボタン不能化。常備化経験点「ー」のアウトフィットはキャスト（とキャスト所有トループ）へのドラッグ＆ドロップ・インポートを弾く（入手経路はアクト中の購入判定のみ）
- **改造判定**: 改造タイプ用途から起動。対象アウトフィット選択→改造項目の選択（判定前・分類別テーブル準拠・「ー」「解説参照」「改造済み」は不能表示）→判定（目標値式で対象の購入値を参照可能）→成功で専用の改造記録に＋［レベル］を適用。素値は保ち実効値へ合流（ヴィークル制御値修正は最大0・住宅の登場±・防御力/セキュリティ一括）。1項目への改造は技能を問わず1回まで。改造はアクト終了で元に戻り、シートの改造セクションと右クリック「改造を解除」で手動解除もできる
- **ドラッグ改造**: 所持ドラッグを［レベル］個まで選択し、使用タイミングを一時的にマイナーアクションへ上書き（戦闘タブ・記帳が実効タイミングで追随）
- **改造して入手**: 購入用途の属性。オンの購入は判定前に改造項目を選び、成功で改造済みのアウトフィットを入手する
- **リファレンス・リンク埋め込み**: リッチテキストに辞典アイテムやロール表への参照リンクを埋め込める（エディタへのドラッグで挿入・クリックでシート表示）。リンクは辞典カード・シート・チャットカード・情報項目・ハンドアウト・シナリオシートなど全表示面で解決され、辞典アイテムへのリンクはホバーで辞典カードのツールチップを表示
- **コアUIのN◎VAテーマ化**: チャット・ボタン・サイドバー・シーンコントロール等の Foundry 標準UIをトーキョーN◎VA の配色に統一
- **サンプルPC辞典**: キャスト用のアクターコンペンディウム（ブラウザには載せない）
- スロットゲージを持つアイテム行は名前＋ゲージの二段表示（改造等で容量が増えても見切れない）
- 経験点配布ダイアログに**「全て自動で入力する」ボタン**。チェック8種を全て ON にし（よほど厳密に裁定しない限り取得条件は全て満たされたものとして配布するのがほとんど、という卓の運用実態）、神業＝そのアクトで消費した回数（`uses.spent` の合算）、登場＝アクト中に登場したシーン数の実測値を全 PL 行へ一括入力する（合計側の上限5は従来どおり）。記帳は従来どおり「確定」まで行われず、RL は個別に修正できる
- 登場シーン数の記帳機構: 登場フラグの変化をアクティブ GM が購読し、キャストごとに「登場したシーン数」を sessionState に累積する（1シーン1回・同一シーン内の再登場は数えない・巡回シーンの再入場は別シーン）。アクトの読み込みでリセット

### Fixed
- プレアクト購入フラグ（isPre-play）が経験点計算で読まれておらず、立てても常備化経験点が計上されていた
- スロット容量の表示（占有インジケータ・ゲージ・実スロット判定）が素値を読んでおり、改造・効果による増減が反映されなかった
- 大分類固定アウトフィットの分類表示の縦位置ずれ・プルダウン長の不揃い
- 経験点配布ダイアログの RL 経験点の計算式を是正。正しくは「会場の手配（1点）＋プレイヤーの取得経験点の合計 ÷『3かプレイヤー人数の小さいほう』（切り捨て）」であり、従来の「PL合計÷3 と PL人数の少ない方」は誤り。RL 行に残っていた擬似コードのメモ表記を廃し、実数の内訳表示に改めた（会場チェックは「会場」列に整列）

## [0.15.0] - 2026-08-30

フェーズ15（時間・使用回数管理）完了。フェーズ13-6 と 14-2 が**発火するだけ**にしておいた境界イベントに、初めて購読者が付いた。効果の持続・使用回数・状態の回復を、TNX の時間単位（メインプロセス／カット／シーン／アクト）の境界で自動的に畳む。

### Added
- **時間境界の適用本体**: 境界イベントの購読口を1本に集約し、境界ごとの手続きを各機構に散らさない。「何が・いつ・どう畳まれるか」は宣言側（効果の持続欄・`uses.type`・状態レジストリ）に置き、ディスパッチャは宣言を読んで適用する。適用は activeGM のみ・黙って行う（ダメージを除く）
- **効果の持続時間**: 効果設定シートの持続時間タブで **メインプロセス中／カット中／シーン中／アクト中** を指定でき、その境界で自動失効する。効果一覧の「効果時間」列に表示。Foundry 標準のラウンド/秒は本システムでは動かない（実時間を使わない）ため置き換えた。失効するのはアクターに乗った効果だけで、アイテム側の定義は消えない
- **退場＝そのキャラクターにとってのシーンの終わり**: 一度退場したら再登場はできないため、「シーン中」の効果と使用回数は境界を待たず退場時に畳む
- **使用回数の自動リセット**: 「カット中N回」「シーン中N回」「アクト中N回」を各境界で戻す。神業は期間の指定が無くてもアクト単位。**消費アイテムの個数**はアクト終了で常備化個数まで回復し、**故障・破壊**も解除される（アクト間に持ち越さない）
- **バッドステータスの回復**: 恐慌＝本人のメインプロセスの直前、萎縮・憎悪＝本人のメインプロセス終了、酩酊（小）・電子妨害＝クリンナップ、酩酊（大）＝クリンナップで（小）に変換。カット進行の終了とシーンの変わり目で全解除。「治療するまで回復しない」BS は元の負傷が残っている間だけ回復を飛ばす
- **行動を支払って回復する BS**: 重圧・狼狽＝マイナーの使用、捕縛＝メジャーの放棄、邪毒＝マイナー・メジャーの放棄を一度に宣言。戦闘タブに 1 BS 種別 1 ボタンで並び、押した時点で支払いと回復が済む。メジャーの放棄は行動を行ったものとみなし AR を消費する。押せるのはカット進行中のみ
- **戦闘不能の解決**: 気絶・失神はカット進行終了で自動回復。仮死・昏睡はシーン終了までに治療されなければ完全死亡。**行動不可**（仮死/昏睡の治療後2シーン）を新設——その間は登場判定が自動失敗になり、舞台裏の手番も回らない
- **ポストアクトの後始末**: 残存ダメージ（負傷と非終端の状態）を消去し、終端状態は残す。終端状態を持つキャラクターを**ロスト確認**として提示する
- **邪毒の継続ダメージ**: クリンナップのたびに受付カードが出て、**受けたキャラクターを操作しているプレイヤーか RL が「山札を引く」を押す**（自動では引かない）。「出た数字 ＋ 強度」点の肉体ダメージを適用し、同じカードに引いたカードと内訳を表示する
- **社会ダメージ系の自動化**: **逮捕令状**＝チームから抜けてから退場し、以後は登場判定が自動失敗（負傷は次のシーンが終われば消える）。**「次のシーン」効果**（信用失墜・スキャンダル・信頼喪失）の発火機構——発火側が無かったため、これらは永遠に休眠したままだった

### Changed
- 状態レジストリの宣言を `resolveAt` / `becomes` に統一（境界での「解決」は回復とは限らず、仮死→完全死亡の悪化も同じ機構で表す）
- 登場判定の強制失敗が理由コードを受け取る形になり、行動不可・逮捕令状が同じ口に乗る

### Fixed
- FS の支援 AE が持っていた Foundry 標準の持続時間（実際には動かない指定）を TNX の持続へ置き換え。使われなかった支援がカット終了で消えるようになった
- バッドステータス「狼狽」の回復条件が正本に記録されておらず、コード内コメントにだけ残っていた（回復＝マイナーアクションの使用）。記録を補填し、回復導線を出すようにした

## [0.14.0] - 2026-08-27

フェーズ14（セッション進行）完了。フェーズ13 が戦闘シーン1種のカット進行を実装したのに対し、14 は**非戦闘を含むシーンとアクト全体のライフサイクルを一般化**した。シナリオコントロールパネルを卓の運用中心に据え、アクトの開始・進行・終了、登場判定と登場表現、チーム、サブシーン（盤面状態）、舞台裏スポット巡回、情報収集判定（旧フェーズ16 を統合）までを実装した。アクトシートは台本の編集専用となり、実行系はすべてパネルへ移った。

### Added
- **アクトのライフサイクル**: 読み込みと開始の分離。**アクト開始トリガー**＝報酬点（`bountyBase` ← 外界点実効値・`bounty` リセット＝清算を兼ねる）と CS の全キャスト自動設定、キースタイルの識別キー⇔カード画像名による**切り札の自動配布**、キー被りが残っていれば開始をブロック。**アクト終了**＝経験点の半自動配布のあとにアクト限定技能の後始末（確認ダイアログ・既定は削除／維持したものは `isActLimited` と初期取得フラグを落とす）
- **シーンのライフサイクル**: RL の開始/終了宣言、シーンプレイヤー（台本ではハンドアウト参照で指定＝担当が変わっても台本を書き換えない）、シーンカードの提示、舞台エリア（セキュリティランク）。リサーチは固定シーンに加えて**巡回シーン**（ハンドアウト順にシーンプレイヤーを回す）と**イベントシーン**を表現でき、シーン番号は自動カウント
- **シナリオコントロールパネル**: シーンコントロールバー起点、アクトシートからのデータ駆動（手動設定なし）。**固定ヘッダ＋使用頻度別の3タブ**（進行／キャスト／アクト）で、現在シーンの表示（フェイズ・SCENE n・シーンプレイヤー・登場の技能と目標値・シーンカード・登場中）はヘッダに常駐。開始前と上演中でセクションを出し分け、タブと折りたたみの状態を永続化
- **境界イベント**: アクト／シーンの開始・終了イベントを発火（`tnxActStart` / `tnxActEnd` / `tnxSceneStart` / `tnxSceneEnd`）。購読側＝効果の失効・使用回数のリセットの適用本体はフェーズ15
- **サブシーン**: 名前付きの盤面状態（背景）をワールド単位で保存・切替する独立機能と専用パネル。シーン行から舞台（サブシーン／通常 Scene）を参照して切替時に適用
- **登場判定**: エリア別の目標値、危険値の達成値加算、サンクチュアリ、ゴースト宣言、専用チャットカード（種別タグ・エリア行・成功時の帰結行）。**判定そのものはブロックせず、条件を満たさない場合は強制失敗**（達成値によらず失敗＝手札を入れ替える権利は誰にも等しくある）
- **登場表現**: 未登場＝トークン無し／登場＝トークン有り／**ゴースト登場＝不可視**／退場＝トークン削除。退場は確認ダイアログを経る。**チームの退場連動**をゲーム設定で選択でき（既定オフ）、確認は連動が自分以外に及ぶときだけ出る
- **RL による登場**（登場判定なし）: パネルからキャラクターを直接登場・退場させられる。**名前の非公開**（卓には「？？？」）と、シーン行への**登場キャラクターの事前設定**
- **チーム管理**: 結成・参加・離脱・編成。宣言はいつでも可
- **舞台裏スポット巡回**: 非登場キャストへ順にスポットを回し、済みを記帳する枠組み（強制はしない）。購入判定・情報収集判定はこの枠組みに刺さるインスタンス
- **情報収集判定**（旧フェーズ16 を統合）: 公開状態3段階（非公開＝存在のみ／公開＝項目名・使用技能・目標値／開示＝内容）。HUD への情報項目表示（非公開も「非公開の情報」として存在を示す）とチャットカード意匠のツールチップ常設、PL の起動導線、**判定成功で達成値が抜いた目標値まで一括自動開示**、新たに判明した分の公開カードの自動送信。回数制限は設けない
- **情報項目の段**: 1つの項目に「追加で判明する内容」を目標値つきで積める（段が開けば入口も開く累積規約）
- **指定技能への応答の統合**: 指定技能／代用技能／**指定充足宣言**／行ごとの代用判定を、縦積みボタンのダイアログ1回に統合（情報収集・判定要求の両面で共用）。スタイル技能に「指定技能として扱う」宣言、一般技能に**社会の下位区分**と固有名詞技能の区分を新設し、名前欄は区分プレフィックスを自動表示
- **判定要求の複数指定**: 指定技能を複数指せるようになり、スタイル技能・ワークス技能も指せる
- **HUD**: ステータスパネルの移設と参加者パネルの新設、カードの D&D 移動の拡充、登場三態のステータス表示

### Changed
- **アクトシートを台本の編集専用に**: 設定タブとセットアップウィザードを完全廃止し、チャット送信・判定起動などの実行系をすべて削除。レイアウトを確立意匠（fieldset ＋ ラベル列グリッド）へ全面改修し、長文欄をテキストエディタ化。シナリオテキストは RL プリセットタブへ合流
- **チーム経由の自動登場をオミット**し、登場の適用を RL 操作に一本化（チームの登場資格は卓で運用する）。ゴーストのトグルを追加
- **アクトコネクション**: ハンドアウト送信カードのボタンで受け取る形に変更し、指定方法を PC ／ NPC ／自由記述の3モードへ
- **シナリオコントロールから送信するチャットカード**（シーン切替・トレーラー・シナリオテキスト・情報・ハンドアウト）を、判定要求・報酬点カードと同じ骨格の意匠に統一
- **情報項目の目標値は等価**として表示（低い＝主／高い＝追加という上下関係を作らない）。指定技能の表示規則を1つの関数に集約
- **HUD 全体を意匠統一**（ステータス表示・情報項目のプレート意匠へ）
- 使用回数の最大値を式で入力できるように（`uses.maxTotal` 新設）
- 判定バフの ActiveEffect に社会の下位区分キー（`check.society.<区分>`）を追加
- キャストシートの経験点履歴を、そのキャストのセッション分だけに絞り込み

### Fixed
- 経験点を所持量を超えて消費できた不具合（KI-040）
- 経験点の記帳日付が UTC 基準で JST 午前に前日となる不具合（KI-041）
- `activeGM` 不在時に情報収集判定の自動開示が消失し、結果カードだけが「情報を開示した」と表示していた不具合（KI-042）
- 実効値でなく素の値を表示していた箇所の一掃（キャストシートの登場列・技能レベル・コンバイン行など。KI-039）
- 枠（fieldset）が中身の長さで横に伸びて親からはみ出す問題の構造的是正
- パネル本文のスクロール位置が再描画で先頭に戻る不具合
- 情報の「目標値の送信」が本文まで送っていた不具合
- サブシーンの適用でシーンの読み込みが走っていた問題（背景をテクスチャ差し替えに）
- 閲覧モードと編集モードでアイテム名の縦位置がずれる問題（全アイテム共通・同一部品化による構造的解決）
- ダイアログのチェックボックス単独行で長いラベルが途中折り返しになる問題
- ニューロカードの HTML 生成が非推奨のグローバル `TextEditor` を参照していた問題（v13）


## [0.13.0] - 2026-08-06

フェーズ13（カット進行＝戦闘トラッカー）完了。Foundry の戦闘トラッカーを TNX のカット進行へ上書きし、CSカレント・AR の自動記帳、メインプロセスの割り込み（挿入メイン・追加行動）、カット境界イベント、フォーカスシステム（FS判定）の実接続までを実装。AR・メジャーアクション・プロセス所有の一般則を確定し、割り込みと支援判定を再設計した。

### Added
- **カット進行トラッカー（サブターンモデル）**: Foundry の戦闘トラッカーを上書きし、1 カット＝1 ラウンドで進行。メインプロセス＝キャラクターのターン、セットアップ／イニシアチブ／クリンナップ＝全体の場（サブターン）。CSカレント・現在AR の自動記帳（カット開始シード・セットアップ末の CSカレント確定＝一度計算して凍結・クリンナップの AR 全回復）。イニシアチブは「次に誰がメインを行うか」を状態（CSカレント最大かつ AR≥1）から確認する場。用途タイミングを持つ参加者だけにスポット（行動権）を回す絞り込み。待機・行動不能の宣言
- **AR・メジャーアクション・プロセス所有の一般則**: メジャーアクションを行ったプロセス所有者が、そのプロセス終了時に AR−1＋CSカレント0（実際にメジャーを行った時のみ）。AR 減少 ⟺ CSカレント0（行動不能にも適用）。AR≥2 は CSカレント0 の手番で再行動
- **割り込み（挿入メイン）**: 宣言／判定用途の「割り込みを許可」トグルで対象に割り込み権を付与し、トラッカーから順番外のメインプロセスを挿入。元のプロセスは終了せず退避され、割り込み終了で復帰（サスペンド／レジューム・入れ子対応）。用途の「割り込みで AR を消費」宣言で挿入メインの AR 消費可否を決定（自己割り込み＝有償・追加行動技能＝無償で肩代わり）
- **カット境界イベント**: カット進行（＝シーン）／カット／プロセスの開始・終了イベントを発火（効果失効・使用回数リセットの購読基盤）。カット進行終了時にシーンも終了するかの確認ダイアログ
- **FS判定（フォーカスシステム）の実接続**: 進行判定の結果→獲得進行値の自動加算（`floor(差分値÷10＋進行修正)＋支援`）、進行修正の解決（固定／式／アクター値／準備済みアウトフィット）、支援判定の成功→対象キャラへ進行ボーナスの ActiveEffect 付与（進行判定時に合算・消費）、メインプロセス開始で進行判定・イニシアチブ開始で支援判定を自動送信、カット経過に連動するカウントダウン表示
- **RL 任意ダメージの拡張**: ダメージ算出の直前フェーズ、固定値／カード算出の選択
- **アウトフィットのオプション装備の統合**（別大分類ホスト対応）＋準備ゲート

### Changed
- 動転（バッドステータス）による制御判定要求を自動化。負傷データを整理
- 効果シートの属性キー入力を独自コンボボックス化し、候補を全 ActiveEffect キーに網羅

### Fixed
- FS判定パネル: カットの ＋／− の向き（表示＝残りカットに合わせる）、支援判定の目標値表示、外部更新での再描画、進行ボタンのレイアウト崩れ、FS 文脈の読み取り（進行ボタン・支援が発火しない不具合）
- 「効果の下書き」アイテムがワールドに残り、ロード後にアイテム一覧へ出る不具合

## [0.12.0] - 2026-07-21

フェーズ12（攻撃判定 + ダメージ算出 + FS判定基幹）完了。判定システムを攻撃・リアクション・対決解決まで単体で完結させ、ダメージ算出（フェーズ9 の ActiveEffect / チャート基盤へ接続）、フェーズ11 申し送りの heads 自動減算、FS判定（フォーカスシステム）の基幹を実装。カット進行の割り込み管理・進行判定の自動加算はフェーズ13 へ分離。

### Added
- **攻撃判定（attack 用途）**: アイテムロール／使用ボタンからの起動、武器解決（参照武器→生身フォールバック）、命中判定、対決解決（相手の達成値を目標値に命中判定）。系統別のリアクション導線（物理＝ドッジ／パリー、精神・社会＝リアクション）。カードのライブ書き換え（ボタン領域を成否表示へ置換）
- **ダメージ算出**: 命中判定とは別のダメージカードを出すのがトリガー（手札は HUD 直接クリック・山札は 1 枚めくり・複数枚は合算・ジョーカー＝ワイルドカード）。攻撃力＋修正−軽減の台帳表示、防御側の軽減ダイアログ（防御力・パリー受け値の自動計上）、`applyDamageChartResult` への配線、damageBoost／damageReduce の適用と消費、スタン／説得（10 上限）、3 系統（S／P／I／X）
- **ダメージ適用の例外系**: トループ級＝heads 減算（チャート不参照）、分身＝消滅通知、エキストラ＝適用不可警告。適用は対象の所有者権限が要るため非所有クライアントから GM へ委譲（`damageUpdate`）
- **代用判定**（指定技能を別技能で任意に代用・ペナルティは手入力）、**治療判定**（負傷／戦闘不能をクリック→〈医療〉判定→負傷群の除去）、**チャート実効果**（腕部損傷の片手持ちペナルティ＝partSlotMod、派生ダメージ＝軽減不可の連鎖適用）
- **リアクターの汎用化**: リアクションの実行者を攻撃対象から分離（同乗者を操縦者が操縦でドッジ等・肩代わり）。押下者＝リアクターとして解決
- **ヴィークル操縦移動**（対応する操縦で判定→達成値÷10 段階・戦闘タブの合成メジャーアクション）、**複数武器の攻撃力合算**、射撃武器の弾数を使用回数へ一本化
- **用途シートの式表示と干渉入力**: 判定ボーナス／ダメージ修正を「式＋供給元」の行として持ち（式で ActiveEffect と同じ値・任意アイテム・対象・`@diff`／`@achievement` を参照可）、用途自身の修正値欄も追加
- **適用効果の2リスト方式**（「適用される効果」＝命中時／「適用される効果（ダメージ時）」）と対象への効果付与（結果／攻撃／ダメージカードの手動「効果を適用」ボタン・複数対象は除外ダイアログ）。アイテムを狙う ActiveEffect の物理転送＋片方向同期、受けるダメージ軽減 AE、対象依存のダメージバフ（vsStyle／vsWorks）
- **リアクションフロー**: 用途の「対象」×「対決」の決定表による対象解決とカード一般化、他者への攻撃への併存リアクション、オープンリアクション（任意・複数）、シークレットカード（GM＋本人）→解決で全体公開、判定結果カードの基底化（結果／対決／リアクション／移動を「基底＋追加情報」に統一）
- **判定要求**: 対象アクター登録方式（所有者権限で応答）、指定技能を組み合わせに含む用途の応答候補列挙＋用途プルダウンの二段階化、「判定」タイプ限定。社会ダメージの報酬点軽減（リアクション成立ゲート）と判定側報酬点の自動減算
- **FS判定（フォーカスシステム）の基幹**: FS判定シート（JournalEntry・起動フォームと共有するエディタ）、進行状態の正本＝ワールド設定 `activeFocusSystems`（起動時スナップショット）、シーンコントロールの FS判定パネル（進行値／カットのゲージ・閾値マーカー・手動増減）、進行修正の参照パラメータ（なし／アウトフィット／キャラクター・`@param`）、進行判定／支援判定の要求カード、達成／敗北の確定と結果カード
- **RL 任意付与（3 種）**: ダメージ（対象・系統・値の直接指定→既存の適用／軽減フローへ合流）、状態・効果（バッドステータス選択／既存効果の複製・供給元と切り離し）、報酬点の配布カード（前金・負数で没収）。アクトシートの「RL プリセット」タブ（下ごしらえ・手動並び替え）
- **用途タイプの行動種別化**（16 種＋リアクション）と戦闘タブのタイミング駆動表示、用途の「戦闘タブに表示しない」フラグ

### Changed
- 技能起動を唯一の関数 `_activateItemCheck` に集約（リアクション／治療／操縦移動／判定要求の迂回を正規フローへ統合＝コンボ・消費・判定ボーナス・適用効果が一律に通る）
- ダメージ種別の表記を S／P／I／X に統一（非正式用語を全廃）、選択肢は定数表から生成
- 対象選択を RL 付与・判定要求の各ダイアログで共通のリスト形式に統一
- FS判定シートを JournalEntry のシートとして作り直し（当初の JournalEntryPage サブタイプ登録＝作成導線を欠く誤りを是正）
- 攻撃で相手に与える効果の付与を、命中・最終ダメージと突き合わせる手動ボタン方式へ（回避対象・最終 0・カバー付け替えの不整合を解消）

### Removed
- 射撃武器の旧「残弾」データ構造（使用回数 uses.max／spent へ一本化・旧データは破棄）
- 既定技能／skillRoles／正準名ブリッジ（用途タイプの行動種別化に伴い撤去・一回限り移行あり）
- ダメージ算出の FA 選択制（FA 値は用途のダメージ修正式で手動参照）

### Fixed
- **KI-024**: スタン／説得の 10 上限の適用位置を恒久軽減後（ダメージ算出の最後）へ是正
- **KI-025**: 判定要求で指定技能を組み合わせた別技能の用途を応答候補に列挙
- **KI-027 / KI-028**: 攻撃の適用効果が命中・最終ダメージと突き合わされない／非攻撃の対決カードで「効果を適用」ボタンが出ない
- **KI-029**: 辞典インポートで素の「判定」用途が正準名リタイプされ別タイプへ化ける（常設移行経路の撤去）
- **KI-030**: 移動・離脱用途で対象選択ダイアログが出る
- **KI-031**: 用途ピッカーで長い用途名がアイコンに被る
- **KI-032 / KI-033**: 修理用途が対象欄を理由に実行不能／用途の「解説参照」が残る（ターゲット関連ブロックの全廃）
- **KI-034**: アウトフィット系アイテムをロールできない（宣言用途が候補から除外されていた）
- **KI-035**: アクトシートの縦型タブがクリックを取りこぼす（タブ子要素を `pointer-events: none`）
- **KI-008**: EXP 再計算の同一アクター並行実行を合流（計算ロジックは不変）。これをもって番号つき未解消 KI はゼロ

## [0.11.0] - 2026-07-08

フェーズ11（guest / troop / extra シート）完了。キャスト共通部品化のうえで残りの Actor 型すべてにカスタムシートを整備し、トループ／エニグマ／分身（トループ級）の本体・作成ルールと NPC取得用途（D&D 召喚相当）、用途の消費先設定を実装。

### Added
- **キャスト共通部品化**: シート partial 8 本＋共有基底クラス `TnxCharacterSheetBase`（cast 残置は EXP 系とセッション履歴のみ）。シートごとの機能差は `SHEET_FEATURES` フラグで宣言
- **guest シート**: キャストからセッション履歴（EXP・User 同期）とライフパスを除いた構成（DataModel 共通基底 `CharacterBaseDataModel`・差分はテストで不変条件化）。RL（GM）手札上限への全ゲスト修正合算・新規作成時のシード（部位プリセット・基本13技能・リンクトークン）
- **troop シート**: 種別ドロップダウン（トループ／エニグマ／分身）、人数／エニグマポイント（トークンリソースバー連携・手動増減）、トループレベル（エニグマでは「エニグマレベル」・能力値＝スタイル基本値＋レベル・制御値も同様）、名前の固定規則（トループ=「(スタイル名)・トループ（Nレベル）」／ワークス設定時=「(組織名)（(スタイル名)Nレベル）」・部署技能による所属名上書き／分身=「(所有者名)の分身」）、スタイル1つ制限、**所有者（取得元）参照**（経験点計上・分身名導出・使用回数共有の紐づけ）
- **extra シート**: 名前・パーソナルデータ・技能（固定値判定用の一般技能のみ）・説明のタブなし1ページ。**固定値判定**（スート・レベル・カード・能力値を読まず固定達成値をチャット出力。作成はエキストラ直下の一般技能のみ・インポート時に用途を自動整形）
- **NPC取得用途**（トループ取得技能・アウトフィット系のみ作成可）: モード明示選択——エキストラ=判定なしで派生取得＋共有エキストラアクターの配置（アウトフィットの二重表現）／トループ・エニグマ=用途側で設定した対象へ通常判定→達成値を人数／ポイントに転記＋レベル転記／分身=対象設定なしの直接召喚（目標値10・召喚数指定・永続1体を本体から**差分反映**（全アイテム複製＋成長の修正値焼き込みで数値完全一致・神業除外））。トークン配置は dnd5e 踏襲（プレビュー追従・クリック確定・要配置権限）
- **用途の消費先設定**（D&D Consumption 踏襲）: 全ての使用回数消費は用途の設定からのみ発生（親アイテム自動消費・コンボ遠隔消費の自動スキャンは全廃）。消費先=親アイテム／特定アイテム／神業（usageCount）・消費量可変。既存 check 用途は「親×1」に自動互換
- **分身の使用回数共有**: 本体側カウンターを唯一の正本とする参照消費（識別キー照合・分身側アイテムシートは共有表示）
- **所有トループ級の経験点算入**: トループ／エニグマ（分身除外）の技能・アウトフィット取得コストを取得元キャストの消費経験点に計上（所有者参照が前提・ゲスト所有は消費なし）。キャスト側に小計行・トループ級シートに所有者経験点を表示
- **発動タブの自動入力拡張**: 用途作成時の一回自動適用＋ボタンで消費行も導出（親×1＋使用回数制限つき参加技能×1）
- **戦闘タブの AR（アクションランク）表示**: 付与型（カット進行中のみ現在値・非戦闘時は「なし」）

### Changed
- スタイル／神業／組織のアクターへのドロップをアイテム種別ルーティングに変更（閲覧モードでも神業同時インポート・各上限検証が働く）
- 用途一覧の使用ボタンを効果の有無にかかわらず常に描画
- 対決表示の識別キーを技能名に解決・開発用語の表記を除去
- `OUTFIT_TYPES` を `OUTFIT_ITEM_TYPES`（helpers）に一本化

### Removed
- スタイル技能の旧「取得アクター」（autoAcquireActors）——取得対象は NPC取得用途側へ一本化
- troop の旧 `sourceName`（分身元テキスト入力）——分身元は所有者参照のライブ解決名から導出
- troop の旧 `memo`・`isEnigmaMode`（biography／troopMode へ移行）

### Fixed
- 用途シートのフィールド保存漏れ（固定達成値）・人数±ボタンの報酬点 NaN 検証エラー・トループ級シートの所有者経験点が一手遅れで同期される問題・消費量スピナーの UI 重なり・チャットカード本文の文字色（暗背景への明色既定）・辞典インポート時にベース技能の自動設定が効かない問題・分身召喚時の基本13技能二重取得

## [0.10.0] - 2026-07-02

フェーズ10（既存シート関連のやり残し総仕上げ）完了。部位管理、スタイル技能の自動取得・取得経験点、住宅エリア表示、combatSpeed 入力 UI（KI-010 解消）、キャストシート戦闘タブを実装。表示中心の仕上げであり、ダメージ算出（フェーズ12）・カット進行（フェーズ13）は持ち込まない。

### Added
- **部位（part）管理**: 部位行の種別ベース化（なし/身体部位/オプション/解説参照/その他）・任意/結合（and/or）、キャストの部位スロット集合（`partSlots`・ゲーム設定プリセット・「指定部位を複数占有」エイリアス）、占有/超過表示（身体部位＋ホストスロット）、オプションの装備先 UI とホスト照合、アウトフィット一覧の準備状態入れ子表示＋スロット使用ゲージ
- **スタイル技能の自動取得**: 習得時に対象武器を複製生成（複数候補はプルダウンで 1 つ選択）・トループはアクター ID 保持（本体生成はフェーズ11）・識別キープレフィックス照合による重複取得防止・武器使用義務フラグ
- **技能の組み合わせ解決**: 技能チェーン解決（アクション技能＝ベース・個数上限＝ベースレベル+1・超過時のトリムダイアログ）、技能/対決欄の辞典カスケード（識別キー格納・表示は都度逆引き）、アイテムのロール挙動（用途なし→説明チャット・不備検知）
- **アウトフィット特性**: 派生データ（本体＝常備化経験点を消費しない・派生元＝取得時に自動生成）・変異器官・式神装備（攻撃/防御/操縦/独立）・「専用」のスタイル/オーガニゼーション辞典参照化
- **取得・経験点**: 経験点消費なし（`expFree`）、秘技/奥義の取得数表示（スタイル別グループ見出し・表示のみ非強制）、他スタイル技能レベルの自動参照（`levelRef`・派生計算で解決＝判定フロー無改変）
- **住宅エリア表示**: 住宅施設名の横にエリアランクバッジ（ライブ解決）、セキュリティランク「イエロー」追加（レッドとグリーンの間）、辞典ドロップダウンのランク順ソート、住宅エリアの常備化経験点修正を EXP 消費へ反映
- **combatSpeed（CS）3層パイプライン**（KI-010 解消）: ベース/CS/カレント＝決定値（再計算不可）＋ライブ修正値のモデル、シート表示は単一「CS」の自動制御（カット進行中＝カレント参照）、カット開始時のカレント自動セット、タップ CS 修正の「ゴースト時無視」フラグ（携帯起点・（）表記）、ActiveEffect の `system.cs.base|value|current` 着地、initiative は表示中の CS（`displayTotal`）を参照
- **キャストシート戦闘タブ**: CS ボックス（公式プロファイルシート様式・カット進行中はグロー・編集モードは「決定の連鎖」縦積み）、攻撃/パリーの参照武器 2 行（`weaponRefs`・生身＝アクターデータ `baseAttack`/`baseGuard`・生身変更装備 `isFleshChange`・「生身（名前）」表記）、防御力の種別ごと合計、タイミングごとの使用技能・アウトフィット再表示（標準 8 種常設・差し込みは非空のみ）
- 表・リスト系 UI の手動並び替えを標準化（部位リスト等・順序を永続化）

### Changed
- スタイル技能シートの設定タブを関心別（分類/発動/特技専用/取得・経験点）に再分類
- オプション判定を部位行（kind=option）からの派生に変更（旧チェックボックスの役割を吸収）
- 「専用」を自由記述から辞典参照の配列へ移行（旧文字列は機械変換不能のため空配列化）
- 身体部位プルダウン・識別キー欄の位置をアイテム間で統一

### Removed
- combatSpeed の旧 `mod` フィールド（3層モデルに役割がないため削除）

### Fixed
- **KI-022**: 辞典で紐づけた住宅エリアが解決されず供給値・バッジが表示されない（`fromUuid` 無条件解決へ統一）
- **KI-023**: ApplicationV2 シートのドロップ配線漏れ（スタイルへ神業・アクトへカードをドロップ可能に）
- コンバイン合成時の部位表記をルールブックの法則「部位1+部位2」に修正
- HUD 読み込み直後の挙動を安定化・下バー開閉アニメーション調整
- ダメージチャート効果文アプリの描画エラー、部位スロットプリセット編集アプリのレイアウト崩れを修正

## [0.9.0] - 2026-06-24

フェーズ9（ActiveEffect / 派生値計算パイプライン）完了。派生値計算（実効値）を一本化し、v2 ActiveEffect 適用モデルとコンディション（バッドステータス・戦闘不能・負傷）基盤を実装。行動制限・継続ダメージ・回復の発火、ダメージ算出本体はフェーズ12 以降に分離。

### Added
- **派生値パイプライン**（`CastDataModel.prepareDerivedData`）: 能力値/制御値の実効値（`total` / `totalControl`）一本化・最終値 0clamp、outfitMod / appearanceModifier の B-2 派生化。KI-021 解消
- **v2 ActiveEffect 適用モデル**: base / total 分離、total へネイティブモード（加算/上書き/乗算）直接適用。値バフ `system.<名前空間>`、判定バフ `check.` / `controlCheck.`、条件付きキー。同一効果の重複適用不可＋重複可チェックボックス
- **判定バフ表示**: 判定結果チャットに「判定ボーナス」と寄与エフェクト内訳（開閉式）
- **コンディション**（`CONDITION_KINDS`）: バッドステータス・戦闘不能・負傷を統合。効果値の固定/可変区別、詳細タブの効果値フィールド、判定・派生への適用（酩酊 / 衰弱 / 重圧 / 電子妨害）
- **ダメージチャート**: 肉体 / 精神 / 社会 ×0〜21 段の負傷名・付与条件を同梱（効果文のみユーザー入力）。効果文の設定アプリ、`applyDamageChartResult`、狼狽 BS
- **状態カスケード**: `inflicts` で別状態を自動付与（状態のみ）。ダメージ由来 AE は効果リスト非表示
- **カードドロー**（衰弱 / 重圧の効果値決定）・**controlNegate**（制御判定成功で無効 / 降格）
- **KI-020 解消**: 手札上限への AE 修正（cast の `handMaxSizeMod` 着地＋3層解決）

### Changed
- `CONFIG.ActiveEffect.legacyTransferral = false`、効果列挙を `allApplicableEffects()` へ
- アウトフィット分類にコードキー＋表示値を導入（`category.melee` 等で指定可能に）

### Removed
- `effectMod` 着地フィールド全廃（v2: バフは total へ直接適用）、旧モードB の死蔵ヘルパー撤去

### Fixed
- 衰弱のスート手動選択 UI 撤去（対象・数字は引いて決まる）
- コンディションの kind を `statuses` から判定（効果値欄が出ない不具合）。1 つの AE が複数 BS を持つ場合に対応
- status 選択のグループ化が v13 `<multi-select>` を壊す不具合を修正（Foundry ファクトリで再実装）

## [0.8.0] - 2026-06-20

フェーズ7（判定システム設計）・フェーズ8（判定システム実装）完了。TNX の基本判定フロー（能力/技能/制御判定）を全面実装。攻撃判定＋ダメージ算出はフェーズ12 に分離。

### Added
- **判定エンジン**（`TnxJudgmentEngine`）: カード値計算・技能判定・制御判定・組み合わせスート積集合計算
- **判定フロー**（`TnxJudgmentFlow`）: シングルトン状態管理。手札クリック・山札判定・報酬点入力・切り札消費をサポート
- **判定ダイアログ**（`TnxJudgmentDialog`）: コンテキスト（種別・使用可能スート・目標値）表示。×ボタンで判定キャンセル
- **RL 判定要求**（`TnxRlRequestApp`）: GM がシーンコントロールから判定要求チャットカードを送信。対象 PL の「判定する」ボタンから起動。socket 経由で結果反映（KI-006 解決）
- **TnxUsageSheet**（`ApplicationV2`）: 用途エントリの専用編集シート。タブ構成（基本 / 発動 / 効果）、`submitOnChange` 自動保存（KI-002 解決）
  - 発動タブ: タイミング・対象・射程・目標値（＋「その他」自由入力）・変更不可（※）・対決不可。参加技能の対決を読み取り「リアクション例」を表示
  - **参加技能から自動入力**: ベース技能＋組み合わせ技能の固有値（対象/射程/目標値/タイミング）を優先度（対象優先度・射程優先度・目標値は最大）で合成して発動欄へ反映（ボタン式）
  - 効果タブ: 種別固有設定（コンボ/武器/ダメージ/改造）＋「適用される効果」を統合。ActiveEffect はセレクトで追加・行から解除（D&D 方式）
- **使用回数の消費**: 判定（用途）起動時に参加技能の使用回数を消費。D&D 方式の消費ダイアログ（原則ブロック・チェック解除で消費回避）、組み合わせ技能の遠隔消費。消費確定は起動時・減算は実行時
- **ソケット基盤**（`TnxSocketHandler`）: `judgmentResult` メッセージタイプで PL → GM への判定結果送信を実装
- HUD のカード達成値プレビュー（ホバー時のみバッジ表示）・空手札エリアクリックで補充
- キャストシートの技能行クリックで技能判定を起動（用途ベースのコンボ自動解決）
- キャストシートの能力値・制御値クリックで各判定を起動
- アクトシートのハンマーボタンから情報収集判定を起動
- `generalSkill` 新規作成時に判定用途（check タイプ）を 1 件自動挿入（`preCreateItem` フック）

### Changed
- **UsageTemplate DataModel 全面改訂**: `_id` / `timing` / `target` / `effects` / `baseSkillRef` / `weaponRef` / `damageType` / `formula` / `damageCategory` / `modifiableParams` フィールドを追加
  - 用途タイプを `check` / `attack` / `declaration` / `damageBoost` / `damageReduce` / `modification` の 6 種に確定
  - `_id` により用途をインデックスではなく ID で管理
  - `migrateData` で既存データに `_id` と `baseSkillRef` を自動付与
- `usage-list.hbs` を `data-usage-id` ベースに変更、種別タグ（バッジ）を追加
- アイテムシートの用途編集を `UsageCreationDialog` から `TnxUsageSheet` に移行
- **usage DataModel に発動系フィールドを追加**: `target`/`targetOther`/`isFixedTarget`・`range`/`rangeOther`/`isFixedRange`・`targetValue`/`targetValueNumber`/`targetValueOther`・`isUnopposable`（スタイル技能と同形）
- **使用回数を消費済み（spent）管理に変更**: `uses.value`（残り）→ `uses.spent`（消費済み、残り = max − spent）。最大値だけ設定すれば残りは自動算出（style-skill / outfit）
- **mixin の静的 migrateData 合成**: `SystemDataModel.mixin()` が template の静的 `migrateData` を concrete モデルでも実行するよう修正（潜在的に死んでいた template マイグレーションを修復）
- `_onStartSkillCheck` を用途ベースに刷新: `baseSkillRef` からベース技能を解決し `skillRefs` + 親技能自動追加でコンボを構成
- RollTable ドロー表を 54 枚連続レンジ（ジョーカー 2 枠・その他各 4 枠）に刷新、ニューロデッキモードを再設計
- タイミング選択肢にダメージ算出・ダメージ適用前後・登場判定・舞台裏を追加

### Fixed
- `_patchUsage` を `mergeObject` から `setProperty` ループに変更（配列フィールドとドット記法キーの更新バグを解消）
- スタンドアロンアイテム（actor なし）で用途シートを開いた際のベース技能名 `(削除済み: ID)` 表示を修正
- 山札判定でカードが裏向き状態になりスートが null になるバグを修正
- 発動タブの条件付き入力欄（その他・数字）が、制御セレクトを別の選択肢に変えても古い値が残るバグを修正（変更時にリセット）

## [0.7.0] - 2026-06-14

フェーズ6（未実装シート追加）完了。outfit 系 Item 11 種・lifePath・キャストシートのアウトフィットタブを実装。

### Added
- 全 Item 型（未実装 12 種）に識別キー（identificationKey）を追加
- outfitBase に isCarrying（携帯中）・parentSlotKind・combineGroupId フィールドを追加
- **TokyoNovaOutfitSheet**（アウトフィット共通シート）を新設・general/weapon/armor/ianus/cyborg/tron/tap/vehicle/residence/combiner 10 種に登録
  - 説明タブ（型別概要サマリ + ProseMirror）/ 設定タブ / 用途タブ / エフェクトタブ
  - 準備済み・携帯中・プレアクト購入のヘッダーアイコントグル
  - 大分類→小分類の連動カテゴリドロップダウン（型に有効な小分類のみ提示）
  - タイミング・スロット・消費アイテム個数・modeValueField 系フィールドの編集 UI
- **TokyoNovaHousingAreaSheet** 新設（説明 + 修正値 7 種、住宅エリア辞典 compendium）
- **TokyoNovaLifePathSheet** 新設（説明 / 設定の 2 タブ）
- キャストシート「アウトフィット」タブを全面実装
  - 大分類別グループ（武器/防具/サイバーウェア/トロン/ヴィークル/住居/その他）+ グループ別固有列
  - 行内 isCarrying / isPrepared フラグトグルボタン
  - グループ内ドラッグ＆ドロップ並び替え
- キャスト DataModel に isGhost / bountyBase / baseGuard / appearanceModifier / outfitMod フィールドを追加
- アウトフィット集計の自動更新（createItem / deleteItem / updateItem フック + ready 時初期化）
  - 携帯中アウトフィットから `appearanceModifier`（危険値合算）を自動集計
  - 準備済み・携帯中フィルタで `outfitMod.control`・CS 修正（`outfitMod.combatSpeed`）を自動集計
  - `isGhost` 変更時に `outfitMod.combatSpeed` を再集計（ゴースト登場中は CS 修正無効化）
- キャストシートに報酬点 ±1 ホバーボタン・bountyBase 再計算ボタンを追加
- 住宅施設の住宅エリア紐付け（compendium ドロップダウン / アイテムドロップの 2 方式）
- コンバイナーのコンバイン連携（source1/source2 ドロップ・見た目選択・パラメータ取捨選択 UI）
- 名称マーカー表示ヘルパー（★/†/※/＠/末尾※、`tnxDecoratedName`）
- 住宅エリア辞典 compendium（`housing-areas`）を system.json に追加

### Changed
- outfitBase: buy / hide を `{mode, value}` SchemaField 化（mode = none/value/reference）
- outfitBase: preserveExp / appearancePenalty を `{mode,value}` 化（「なし/数値」2 状態に変更）
- outfitBase: majorCategory / minorCategory を choices 付き StringField 化
- outfitBase: timing を配列から単一 SchemaField に変更（アウトフィットのタイミングは一つのみ）
- outfitBase: part を `[{value, slots}]` 配列化（複数部位占有に対応）
- outfitBase: hack を nullable NumberField 化（なし=null、「-」表示）
- outfitBase: isOption を追加（別アウトフィットに装備するオプション品）
- outfitBase: isConsumption + quantity `{value,max}` で消費アイテムを一般化（旧 isthrow を廃止）
- extensible: slots[] を `{kind, count: {mode,value}}` プール方式に再設計
- weapon: attack を `{damageType, value}` 化（damageType は単一選択 S/P/I/X）、range を `{min, max}` の選択式に変更
- armor/cyborg/ianus/tap/vehicle 各フィールドを `{mode,value}` 化（guardValue/defence/controlMod/cycle/combatSpeedMod/speedFactor/passenger）
- `modeValueField()` ヘルパーを outfit-base.mjs 局所定義から helpers.mjs へ昇格（全モデルで共有）
- combiner: combinedOutfitID[] → combine `{source1, source2, appearance, params}` + isCombineActive に再設計
- lifePath: lifePathType の choices を確定（origin/experience/encounter + 空文字）
- troop: heads を `{value, max}` SchemaField 化
- キャストシート「判定」タブを「戦闘」タブに改称（内容整備はフェーズ11）
- キャストシートのアイテムリストで modeValueField 系フィールドを「-」または値で表示
- ProseMirror エディタをコンテンツ高に自動伸長

### Removed
- cast: player_name フィールドを削除（デッドフィールド確定、フェーズ6-0）
- outfitBase: isBiological を削除（カテゴリ「生体装備」で識別する方針に変更）
- housingArea: hideMod を削除（住宅エリアは住宅施設の隠匿値を修正しない）
- 全シートのフットノート（sheet-notes 注釈）を撤去（title 属性か wiki で代替）

### Fixed
- 全アウトフィット型に `static migrateData` を実装（旧 NumberField 形式 → `{mode,value}` 自動移行）
- キャストシートのアウトフィットリスト部位表示を新 `{value,slots}` 構造に追従
- isCyber ※マークをサイバーウェア大カテゴリには付与しない修正

## [0.6.0] - 2026-06-12

フェーズ5（修正・軽微な機能追加）完了。

### Added
- キャストシート詳細タブ
  - ライフパス 3 スロット（出自/経験/邂逅）。`lifePath` を `SchemaField({itemUuid, name, summary})` 構造に拡張し、ドラッグ＆ドロップでの割り当て・削除に対応
  - キャラクター説明 ProseMirror エディタ（シートの編集/閲覧モード切替に連動）
- HUD 機能追加
  - カラムごとの折りたたみ（両カラム flex 統一、収納時は完全に幅 0）
  - アクセスカード表示エリア（デフォルト折りたたみ）と、クリックで全員に画像提示する使用システム
  - プレイヤーの手札表示エリア（開示設定 `showPlayerHands`・カードを渡す機能込み）
- カードセットアップを設定メニューへ移行（`TnxCardSetupApp`、山札/手札/切り札/その他の 4 タブ、RL用切り札捨て場新設）
- ドロー表: RollTable のカードドロー拡張（コア保持のフック注入方式。仮想 DoC モード + 設定デッキモード）
- 報酬点（bounty）フィールドと一般技能の「報酬点を使用可能」フラグ
- 全実装済みアイテム型に識別キー（identificationKey）を追加
- 一般技能・スタイル技能のコンテキストメニューに「複製」を追加（初期技能は複製不可、名乗り初期分の複製は通常技能化）
- チャット通知のデフォルトを「通知バッジ」に変更（`core.chatNotifications` のデフォルト上書き）

### Changed
- 技能セクションの編集モードを閲覧モード踏襲のレイアウトに再設計
- キャストシートのコンテキストメニューを「閲覧/編集/削除」に統一（一般技能の閲覧モードに縦三点リーダーメニューを追加）
- 他プレイヤーへのカード渡しダイアログを Actor 選択 → User 選択に変更。切り札配布も User 直接配布化
- 所属未設定時の表示を「無所属」から「フリーランス」に変更
- 報酬点エリアをデータパネル風デザインに刷新

### Fixed
- 既存シートの表示バグ一式（コンテキストメニュー不動作、アイテムドロップ、スタイル技能シート各種、役割インジケーター再設計）
- v13 非推奨警告を全解消

## [0.2.0] - 2026-05-24

### Added
- フェーズB-1: Actor 共通 template の DataModel 化
  - `BiographyTemplate`, `AttributesTemplate`, `ActorBaseTemplate` を `scripts/data/actor/common/` に実装
  - `SystemDataModel` 基底クラスおよび `mixin()` ユーティリティを `scripts/data/abstract.mjs` に実装
- フェーズB-2: Item 共通 template の DataModel 化
  - `BaseTemplate`, `UsageTemplate`, `SkillBaseTemplate`, `OutfitBaseTemplate`, `ExtensibleTemplate` を `scripts/data/item/common/` に実装
- フェーズB-3: 単純な Actor type の DataModel 化
  - `GuestDataModel`, `TroopDataModel`, `ExtraDataModel` を実装し `CONFIG.Actor.dataModels` に登録
  - `template.json` から guest / troop / extra エントリを削除
  - テスト 63 件追加(計 201 件)
- フェーズB-4: 複雑な Actor type の DataModel 化
  - `CastDataModel`, `PlayerDataModel` を実装し `CONFIG.Actor.dataModels` に登録(全 5 type 完了)
  - `template.json` の Actor セクションを `types` 配列のみに整理(templates / cast / player エントリ削除)
  - `tests/setup.mjs` に `MockObjectField` 追加
  - テスト 57 件追加(計 258 件)
  - `docs/FUTURE_CONSIDERATIONS.md` 新設(Player Actor → User ベース移行の検討事項)
- フェーズB-5a: 単純な Item type(base のみ)3 種の DataModel 化
  - `HousingAreaDataModel`, `OrganizationDataModel`, `LifePathDataModel` を実装し `CONFIG.Item.dataModels` に登録
  - `template.json` から housingArea / organization / lifePath エントリを削除
  - テスト 25 件追加(計 283 件)
- フェーズB-5b: base + outfitBase の Item type 4 種の DataModel 化
  - `ArmorDataModel`, `CyborgDataModel`, `CombinerDataModel`, `GeneralDataModel` を実装
  - `scripts/data/item/helpers.mjs` 新設(`defenceField()` ヘルパー)
  - テスト 41 件追加(計 324 件)
- フェーズB-5c: base + outfitBase + extensible の Item type 3 種の DataModel 化
  - `IanusDataModel`, `TronDataModel`, `VehicleDataModel` を実装
  - テスト 23 件追加(計 347 件)
- フェーズB-6a: outfit 系 Item type 3 種の DataModel 化
  - `WeaponDataModel`, `TapDataModel`, `ResidenceDataModel` を実装
  - `helpers.mjs` に `attackField()` ヘルパーを追加
  - テスト 49 件追加(計 396 件)
- フェーズB-6b: skill 系 Item type 2 種の DataModel 化
  - `MiracleDataModel`, `GeneralSkillDataModel` を実装し `CONFIG.Item.dataModels` に全 15 type 登録
  - テスト 37 件追加(計 443 件)
- フェーズB-7a: style の DataModel 化
  - `StyleDataModel` を実装(ファイルローカル `abilityField()` で reason/passion/life/mundane の 2 フィールド構造を定義)
  - テスト 28 件追加(計 471 件)
- フェーズB-7b: styleSkill の DataModel 化(全 17 Item type 完了)
  - `StyleSkillDataModel` を実装し `CONFIG.Item.dataModels` に全 17 type 登録
  - template.json 未定義だがシートが参照する ★ フィールド 8 個を明示定義
    (maxLevelNumber / maxLevelOther / targetOther / rangeOther / targetValueNumber / targetValueOther / comboSkillOther / confrontationOther)
  - テスト 60 件追加(計 531 件)
- フェーズB-8: Card type の DataModel 化(全 3 type 完了)
  - `CardBaseTemplate` を `scripts/data/card/common/` に実装
  - `PlayingCardsDataModel`, `NeuroCardsDataModel`, `OtherDataModel` を実装し `CONFIG.Card.dataModels` に登録
  - テスト 12 件追加(計 542 件)
- フェーズB-9: description の HTMLField 化 / template.json 廃止
  - `BaseTemplate`, `CardBaseTemplate`, `BiographyTemplate` の description を `HTMLField` に変更
  - `tests/setup.mjs` に `MockHTMLField` 追加
  - `tests/template-integrity.test.mjs` を documentTypes ベースに全面書き換え
  - テスト 2 件追加(計 544 件)
- フェーズB-10: 移行後の実機検証完了
  - Foundry v13.351 上でキャラクターシート・アイテムシートの正常表示を確認
  - EXP 計算・カード操作・神業 usageCount の動作に破壊なしを確認

### Changed
- フェーズB-9: `system.json` の `documentTypes` に `"htmlFields": ["description"]` を宣言
  (Item 全 17 type / Card 全 3 type / Actor: cast・guest・extra)
- フェーズB-10: `packs/` を `.gitignore` に追加し、LevelDB ファイルの git 追跡を解除
  (GM が手入力するゲームデータはリポジトリ管理対象外)

### Removed
- フェーズB-9: `template.json` を廃止(type 定義の権威が `system.json` の `documentTypes` に一本化)
- フェーズB-10: `packs/` 配下の LevelDB ファイル群を追跡解除(git rm --cached)

### Fixed
- KI-007: tap アイテムの `conbatSpeedMod` タイポを `TapDataModel` で `combatSpeedMod` として正しく定義(B-6a)
- KI-016: `OutfitBaseTemplate` の `exclusive` / `hide` フィールドが `StringField` であることをユーザー確認で確定(B-9)

## [0.1.0] - 2026-05-21

### Added
- フェーズA: 開発基盤の整備
  - ライセンス・著作権・コントリビューション関連ドキュメント (A-1)
  - 設計ドキュメント基盤 (MECHANICS_AUDIT.md, DESIGN_REVIEW.md) (A-2)
  - テスト基盤 (Vitest) およびリント基盤 (ESLint) (A-3)
  - GitHub Actions による CI (A-4)
  - バージョニング・リリース方針 (A-5)

### Fixed
- KI-014: switch case 内の lexical declaration を解消
- KI-015: 等価比較演算子 `==` を `===` に変更

## [0.0.0] - 2026-05-20

### Added
- フェーズ0: v13 動作基盤の整備(初期コミット〜フェーズ0 完了サマリーまで)
