# CLAUDE.md

## 0. このファイルの位置づけと権威の分担

本ファイルは、本プロジェクト(tokyo-nova-axleration / TNX)の開発運用における**規範と指針の正本**である。Claude Code は作業着手時にまず本ファイルを読むこと。

情報の権威は、序列ではなく**領域**で分担する。

| 領域 | 正本 |
|---|---|
| 開発の規範・指針・禁止事項 | 本ファイル(CLAUDE.md) |
| 知識・記録(設計判断の経緯、ルール解釈、既知不具合、フェーズ記録、アーキテクチャ詳細、ロードマップ) | llm-wiki |
| 実装の事実(型定義、フィールド、挙動、互換バージョン) | 実コード・system.json |

本ファイルは知識・記録を**直接複製せず**、必要な箇所へ参照で誘導する(§5)。本ファイルや wiki が実装の事実について古い記述を含む場合は、実コードを正とし、該当箇所の更新を提案すること。規範そのものに疑義がある場合は、独断で読み替えず、ユーザーに確認する。

---

## 1. プロジェクト概要

TNX は、TRPG「トーキョーN◎VA THE AXLERATION」(5th edition、FEAR 系)を遊ぶための Foundry VTT カスタム ゲームシステムである。最終的に Foundry の公式パッケージ登録での公開を志向し、MVP ではなく実走志向(各機能を最低限でなく作り込む)で開発する。開発規模は長期戦(年単位)を前提に、技術的負債を避けて堅く作る。

- 対応する Foundry VTT のバージョンは system.json の `compatibility` を権威とする。本ファイルにバージョン番号を書かない。
- 本システムのコードは MIT ライセンスで配布し、Foundry VTT の Limited License Agreement for Package Development に従う。
- 「トーキョーN◎VA THE AXLERATION」は「鈴吹太郎」「有限会社ファー・イースト・アミューズメント・リサーチ」および「株式会社KADOKAWA」の著作物であり、本プロジェクトはその二次創作物である。著作権遵守の具体的規範は §4.1 に定める。

ゲームルール上の設計前提(カード判定・シーン制・神業・プレイヤー付与 EXP 等)は、wiki の Game_Mechanics を正本とする(§5)。

ロードマップ・現在のフェーズ・前フェーズの完了内容は wiki の Roadmap / Phases に置く。**現在のフェーズと次の作業対象は、作業着手時に必ず wiki の現フェーズページで確認すること。**

---

## 2. 開発体制とワークフロー

### 2.1 役割分担

- **ユーザー**: TNX のルール知識の言語化(ルールの一次情報の提供)、設計・実装方針の最終判断、Foundry 実機での動作確認、git のコミット承認・タグ付け・push・GitHub Releases の作成。
- **Claude Code**: コード調査・設計・実装・テスト作成・記録(監査/設計レビュー/既知不具合)の一貫した担当。ルールとコードの照合。

ルール解釈そのものはユーザーが権威を持つ。Code はそれを前提に設計・実装する(§4.2)。

### 2.2 作業の進め方

- 各フェーズはサブフェーズに細分し、着実に仕上げる。「最小実装」という発想は採らず、各機能を作り込む。
- **承認は設計の確定時にまとめて取る。** 大きな変更や複雑なシステム(判定・神業など)は「設計フェーズ → 実装フェーズ」に分け、設計フェーズでルールの言語化・データ構造(追加・改名するフィールドを含む)を確定してユーザーの承認を得る。**承認済みの設計に沿うサブステップは、逐一の再承認を要しない**——設計から外れる判断が生じたときに確認する。
- 1 つの作業単位に「複数の無関係な不具合修正を混ぜない」(論理単位でコミットを分ける)。
- 新規機能の実装には、可能な範囲でテストを併設する。

### 2.3 二リポジトリ運用

本プロジェクトは二つの git リポジトリにまたがる。

- **本体リポジトリ**(`tokyo-nova-axleration/`): システムのコード、本ファイル(CLAUDE.md)。
- **llm-wiki リポジトリ**(`tokyo-nova-axleration/llm-wiki/`): 知識・記録の正本。本体から `.gitignore` で除外される。パス別リポジトリ、Obsidian で管理。

運用規範:

- コードの変更は本体に、記録(設計レビュー・メカニクス監査・既知不具合)の追記は llm-wiki にコミットする(§6)。
- 1 つの作業がコード変更と記録更新の両方を伴う場合、**両リポジトリそれぞれにコミットする**。本体のコード変更コミットと、それに対応する wiki の記録更新コミットを、同じ作業セッション内で漏れなく両方仕上げること(本体だけ進めて記録が遅れた状態を残さない)。
- **Code の担当はコミットまで。** push・タグ付け・GitHub Releases の作成は、いずれのリポジトリでもユーザーが手動で行う(以降の節でも同じ)。
- コミット先は main 直コミットとする(個人開発・push はユーザー承認制のため、ブランチ運用の overhead を避ける)。大規模な設計変更で作業を隔離したい場合のみ、ユーザーの指示でブランチを切る。
- llm-wiki で作業するときは、カレントが llm-wiki リポジトリであること(`git rev-parse --show-toplevel` が llm-wiki を指すこと)を確認してから操作する。

### 2.4 開発コマンドと表記

- `npm install` — 依存導入
- `npm test` — テスト全実行
- `npm test -- <ファイルパス>` — 単一ファイルのテスト実行(動かない場合は package.json のテストランナー設定を確認し、本行を実際のコマンドに更新する)
- `npm run test:watch` — テスト監視実行
- `npm run lint` — リント
- `npm run lint:fix` — リント自動修正
- Foundry の起動・リロードはユーザーが手動で行う。
- コード内コメント・JSDoc は日本語で書く(コミットメッセージと同様)。識別子・API 名は英語。

### 2.5 長期離脱後の再開(ユーザー向け手順)

Code は経過時間を自力では知りえないため、長期離脱後の再開時は**ユーザーが冒頭でその旨を宣言する**(例: 「1 ヶ月ぶりの再開」)。宣言を受けた Code は次を行う。

- **2 週間以上**の離脱: 本ファイルと、wiki の現フェーズページを見直してから着手する。
- **3 ヶ月以上**の離脱: wiki から現状を再構築して着手する。

---

## 3. アーキテクチャの要点

本節は Code が踏み外さないための**方向付け**に絞る。構造の詳細・現行の型一覧・機構の設計意図は、実コードと wiki(Architecture / Documents / Game_Mechanics)を正本とする(§5)。

- **DataModel(TypeDataModel)ベース。** `template.json` は廃止済み。**復活させないこと。** ドキュメント型の定義は `documentTypes`(system.json)と `scripts/data/` 集中の DataModel が権威。
- **型の一覧・型数を本ファイルに直書きしない**(フェーズ進行で増減し、陳腐化するため)。現行の型はコードで確認する。**廃止済みの型を復活させないこと。**
- **User は DataModel を持てない。** EXP・履歴・手札関連の権威は User flag にある。flag の読み書きは専用ヘルパー(`scripts/module/user-flag-schema.mjs`)を**必ず経由する**こと。実効値の解決仕組みは wiki の Game_Mechanics を参照。
- 派生値計算(能力値・制御値・アイテム実効値)は `prepareDerivedData` に一本化されており、ActiveEffect はこの計算経路に流れ込む。詳細は wiki の Game_Mechanics / Active_Effects と Architecture / DataModel。
- アイテムのロール処理は usage ドキュメントとして管理される(D&D 5e の思想を踏襲)。usage 関連の構造は判定システムの基盤であり、改変は §4.3 の制限に従う。
- 主要モジュールの見取り図(`TnxActionHandler`・`TnxHud`・`TnxScenarioSheet` 等)は wiki の Architecture を参照。
- 命名規則: ファイル名は kebab-case、クラスは PascalCase、パッケージ ID・フラグスコープは system.json を踏襲する。

---

## 4. 不可侵の規範

以下は環境・フェーズにかかわらず常に守る。**不可逆・対外的な操作、またはルール解釈に関わる判断**は、着手前に確認する。それ以外(可逆でルール解釈を伴わない作業)は、必要なら**前提を明示したうえで進め**、確認のために手を止めない。

### 4.1 著作権遵守

- ルールブックのテキスト、ゲームデータをコードやドキュメントに**無断複製しない**。効果テキストの一字転記、特技・神業等の網羅的なリスト化を行わない。**公開外向けの llm-wiki であっても同様。**
  - ※ここで禁じるのは**原文の逐語転記・創作的表現の複製**であり、ユーザーが言い換えたルール内容・メカニクスを**開発用 llm-wiki に構造的に記録すること**は含まない(判定・ダメージ・神業などすべて同基準で記録する)。著作権配慮は**配布物本体(コード・compendium 同梱データ)への焼き込み回避**で行う(効果文は Item へのユーザー投入・入力機能で対応)。
- 判定ロジック等の実装に必要な範囲の「enum・定数といった**構造的最小限**」の値はコードに含めてよい。
- ルールの**構造・仕組み**(データフロー、状態遷移、判定の関係)の記述、および**ユーザーが引き出して記述したルール解釈**に基づく実装・設計・記録は可。ルールの一次情報はユーザーが書く。Code はそれを前提とする。

### 4.2 ルール解釈の確定

- ゲームルールの解釈に関わる判断を**推測で実装しない**。判定の達成値計算・神業の挙動・セッション進行など、解釈が要る箇所はユーザーに確認する。
- 神業の自動化は計算で導けないため、**設計合意のうえで実装する**(usageCount の保守など既存の機械的挙動は可)。

### 4.3 変更の抑制

- **動いている既存システムを、理解と合意なしに書き換えない。** 「改善のついで」「効率化のため」でのリファクタ・大規模移行・全体書き換えを始めない。必要なら提案にとどめ、設計フェーズで合意してから着手する。一見冗長に見える機構が意図的な設計であることがある(例: EXP 同期機構。理由は wiki の Game_Mechanics / Experience_Points と Issues の該当 KI)。
- **先回りの拡張をしない**(YAGNI)。「将来のために」拡張ポイントを作らず、必要になった時に追加する。現在のフェーズに該当しない作業は着手しない(必要な気づきの共有・提案は可)。
- usage テンプレート・`usage-list.hbs`・用途タイプ選択(`TokyoNovaItemSheet._promptUsageType`)は判定システムの基盤のため、設計議論を経ずに構造を改変しない。
- DataModel のフィールド追加・改名は、設計フェーズでの一括承認に含める(§2.2)。

---

## 5. llm-wiki 参照プロトコル

llm-wiki は別リポジトリのため、Code の起動時自動読み込みには乗らない。したがって、**該当領域の作業に着手する前に、対応する wiki ページを明示的に読むこと。** 本ファイルの規範と、wiki の知識を併せて初めて作業の前提が整う。

入口は wiki のインデックス(`llm-wiki/` の Index / Documentation_Map)とし、**実際のページ配置はインデックスを正とする**。領域別の目安は以下。

- 既知不具合・KI 一覧 → Issues(Active / Resolved)
- 設計判断の経緯・メカニクス監査 → System の Audit_and_Review
- ゲーム機構(EXP・カード操作・判定など) → Game_Mechanics
- フェーズ記録・現在地・ロードマップ → Phases / Roadmap
- アーキテクチャ詳細・DataModel・ドキュメント型 → Architecture / Documents
- リリース手順 → System の Release_Process

**wiki の参照を必須とする条件**(いずれかに該当したら、着手前に該当ページを読む):

- 新しいフェーズ・サブフェーズの作業を開始するとき(現フェーズページ)
- DataModel のフィールドを追加・変更・改名するとき(Architecture / Documents)
- 判定・神業・EXP・カード操作・ActiveEffect に触れるとき(Game_Mechanics)
- 不具合を修正するとき(Issues — 既知 KI との重複・関連の確認)
- リリース作業を行うとき(Release_Process)

上記に該当しない軽微な作業(タイポ修正、リント対応等)では参照を省略してよい。

### 5.1 正本に当てて検証する

実装・修正は、記憶や要約でなく**正本(wiki / コード)の原文に当てて検証してから**行う。仮説は自由に立ててよいが、**動かす前に根拠となる該当行を引用し、文の主語・条件・挙動が一致するかを逐条で確かめる**(数ターン前の自分の言語化も、読み直さずに使うと一般形へ滑るので、原文に取り直す)。

- メカニクスは「入力欄の集合」でなく**挙動そのもの**で符号化する(値が「どう決まり・何をするか」を落とさない)。
- **自分のモデルから書いたテストで自分を検算しない**。同根のテストは誤りを通すので、正本と突き合わせて検証する。

---

## 6. 記録運用

記録の正本はすべて llm-wiki にある。本体リポジトリには記録系ファイルを置かない(`docs/` は廃止済み)。

### 6.1 記録の種別と書き込み先

- **メカニクス監査**(ルール解釈の正しさの記録)と**設計レビュー**(技術的妥当性の記録)は、wiki の System / Audit_and_Review に追記する。**この二つは関心が異なるため、混同しないこと**(ルール解釈の妥当性と、実装の技術的妥当性)。
- **既知不具合(KI)**は、wiki の Issues が**唯一の正本**である。未解消は Active、解消・分割済みは Resolved に置く。

### 6.2 記録のタイミング

- 新規実装時: メカニクス監査・設計レビューに各 1 エントリを追加する。
- 既存実装に影響する変更時: 該当エントリのステータスを更新する。
- フェーズ完了時: **当該フェーズで触れた領域のエントリ**を通読してステータスを最新化する(全エントリの総ざらいはメジャーリリース前に行う)。
- 記録の書き手は Code、最終承認はユーザー。

### 6.3 KI 管理

- 新たな不具合が発覚したら、wiki の Issues の Active に起票する。
- 解消・分割した KI は、Active から Resolved へ移動する。
- KI の正本は wiki の Issues 一箇所であり、他の場所に重複管理しない(従来の複数箇所管理は廃止)。

---

## 7. バージョニング / リリース

- SemVer に従う。現在は 0.x.x(開発中)。
- フェーズ完了ごとに MINOR を上げる。フェーズ間のバグ修正で PATCH を上げる。最終フェーズ完了時に 1.0.0 をリリースする。
- Conventional Commits に準拠し、`<type>: <description>` 形式。主なタイプは feat / fix / docs / chore / refactor / test / style。**description は日本語で簡潔に。**
- バージョン番号は package.json / system.json / git タグの 3 箇所で同期する。バージョンを上げるコミットでは、**package.json と system.json の一致を確認してからコミットする**(将来的には不一致でテストを fail させる自動チェックの導入を推奨)。
- リリースの詳細手順は wiki の System / Release_Process を参照する(タグ付け以降の作業分担は §2.3 のとおり)。

## UI追加・変更時の必須方針（2026-09-13 ユーザー指示）

- ボタン・アイコン・入力欄・配置は、該当画面と類似機能の既存TNX実装を確認してから再利用する。Foundry標準の無装飾ボタンや独自に考えたUIへ置き換えない。
- 小さな操作は既存のFont AwesomeアイコンとTNXクラス（例: `tnx-icon-ctrl`、`tnx-ctrl-strip`）に統一する。既存がアイコン操作なら文字ボタンを新設しない。操作名はツールチップとアクセシブルなラベルで示す。
- RL/PL、アクト開始前/開始後、既存タブの表示条件を確認し、既存の操作の流れに配置を合わせる。新しい保存先や別ウィンドウの必要性と、画面構成の判断を混同しない。
